# Execute the prepared four-source joint response test

Root authorizes this contained experiment under Jacob's September22 handoff,
after reviewing the completed angular continuation (29/30 available; no new
calibration because MMOL1770 has an invalid optimizer trial). The broader
angular score does not retain every original-band decision. This test isolates
the diagnosed frozen-metal force; it is not a promotion or calibration rescue.

Execute exactly the eight tasks in `prepared_v1/manifest.json`, SHA256
`3276a4927093d08e6d780d9b951389d01d5aadf7b93c05261a710c7a275e7324`.
The physical coordinates, limits, common four angular IDs, original starts,
native MACE Hamiltonian and SLSQP settings remain those in PLAN.md. No guard
change, fallback optimizer or extra starting geometry. Invalid searches remain
unavailable. The old preparation-only manifest is retained with this separate
execution agreement in its submission receipt.

For valid paired proposals, extend each completed angular five-geometry pool
with the joint Ca/La proposals. Both metals access all seven geometries, with
exact-coordinate deduplication and unchanged operational origin tolerance.
Reuse every compatible old matrix cell and own-proposal MACE endpoint. Maximum
additional work is eight cross-MACE evaluations and32 native GFN2 singlepoints,
all under the already qualified MaxIter500 policy; no DFT. Preserve the original
and angular pools side by side. Failed required cells leave this extension
unavailable. PLM labels remain unknown.

Judge physical feasibility, residual metal/angular loads, movement/boundaries,
individual composite works and paired contrast changes before any expansion.
Preserved crystal directions are necessary but cannot establish broader
reference fidelity. A new class label for a PLM example is not a success target.
Continued boundary pressure or poor solvent selection argues against this local
search. Do not launch a full-fold follow-up or promote the model from this pilot.

One warm H200/32CPU/200000MiB proposal allocation and existing finite solvent
wrappers. All attempts and allocated costs count, including failed proposals.
Root owns execution/scoring; agents must not submit duplicate chemistry.
