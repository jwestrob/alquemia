# Exact-byte runner path recovery, before any search

All202 native origin evaluations and404 strict scalar origin executions completed.
The first wrapper invoked the origin-stage implementation directory, while scalar
manifests pinned their own nested scalar implementation directory. Both runner and
renderer have exactly the same bytes, but the original receipt validator correctly
rejected the path mismatch. Original collections therefore report zero accepted
scalar cells. They and all output/receipt/manifest files remain immutable.

Recovery checks both actual and declared runner/renderer files by SHA256, then
applies the original full receipt validator using the actual recorded identities.
Manifest hash, task/XYZ/input/output/runtime sidecar, actual executable, convergence
and runtime controls must still pass. The strict TolE/state/rank checks remain.
Returned receipt pins retain their original paths; no output or execution record
is relabeled. No completed molecular calculation reruns.

The first dependency chain produced zero-search/unavailable collections and empty
candidate pools. Its allocations remain part of cost. The empty scalar manifest
was rejected by the ordinary runner; no candidate molecules ran. A zero-task branch
now records an empty solvent index instead of preparing an unexecutable manifest.

Replacement stages reuse all actual original origins in new collection files and
new search/pool directories, with exactly the same101 sources and settings. Future
scalar wrappers invoke the manifest's own implementation directory. Root explicitly
authorized this technical recovery on2026-09-23; finite scope remains unchanged.
