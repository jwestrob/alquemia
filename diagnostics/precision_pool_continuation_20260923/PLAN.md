# Uniform continuation qualification: fixed32 precision pools

## Authorized preparation and question

Root's explicit assignment under Jacob's overnight authorization:
“inventory ONE uniform candidate qualification: exact existing precision34
canonical25+3crystal pools plus full225 Q4Ca2/P385La2/A0A3Ca1/A0ACCa4
(32sources ...). Same fixed3geometry pools; apply existing twofixednative
continuations to every metal/media cell, not only problematicones ... Inventory
real GBW+xtbw pairs and compatible completedcontinuations; preserve everymissing
seed explicitly, noautomaticcoldrecomputation ... Prepare finite manifest/count/
costscale beforelaunch.” This document records that finite design. Molecular
execution has not begun; readiness/counts go to root for launch coordination.

Question: does a uniform two-pass continued-energy protocol retain canonical
PQQ class separation and reduce electronic initialization sensitivity at useful
cost? The preceding eight-call diagnostic settled two large geometry-dependent
vacuum jumps. It did not evaluate full pools or biological accuracy, and all
outputs still exceeded printed density-change thresholds.

## Exact sources and invariant model

Take the25 rows designated `calibration` and1H4I/4MAE/1KB0 from
`workspaces/slsqp_precision_expansion_20260923/COMPARISON_v1.json`, resolving
each actual precision collection (including the four-pilot reuses).
Take the following four from the completed precision225 ledger:

- q4w6g0-pqq-la_model__conditioned_Ca__seed-1_sample-2
- p38539-pqq-la_model__conditioned_La__seed-1_sample-2
- a0a3f2yly8-pqq-la_model__conditioned_Ca__seed-1_sample-1
- a0acd6b9f2-pqq-la_model__conditioned_Ca__seed-1_sample-4

No exact case duplicate is expected; fail rather than silently count one twice.
Every source retains the existing origin/adaptive_Ca/adaptive_La geometries,
same union context, nuclear coordinates, charge, protonation, water inventory
and native-MACE energies. No search, new candidate, molecule or high-level call.

For each32×3×2metals×2media=384 cells, use its own actual cold GBW+xtbw pair,
then its own first-pass pair. Existing ORCA native GFN2, ALPB(water) or vacuum,
electronic300K, native mixer, MaxIter500, no TightSCF or cross-seeding. Both
continuations use the verified matching-basename AutoStart route and require
actual XTBRESTART, parameter/state/receipt checks. Final stage is always pass2,
including when it is higher. Missing initial seeds remain missing; no automatic
cold recomputation. Failed pass1 cannot seed pass2. No third pass.

Exact completed two-pass cells may be reused only with identical source
coordinates, charge/multiplicity, medium, parameter export and both cold seed
hashes. This avoids substituting a different initialized electronic solution.
Scalar-only reuse keeps prior analytic-gradient cells distinct. Old eight-rank
receipts remain explicit; new scalar work uses the separately qualified
one-rank profile. No native/standalone backend mixing.

## Frozen analysis

Retain cold/pass1/pass2 raw components, same-geometry work and mathematical plus
operational candidate choices. Composite energy remains MACE_vac+GFN_ALPB−GFN_vac;
R remains E_Ca−E_La. Operational origin preference retains0.1 kcal/mol tolerance.

Necessary energy stability: every pass1→2 cell change ≤0.1 kcal/mol, every
paired same-geometry contrast change ≤0.2 kcal/mol and each pooled contrast
change ≤0.2 kcal/mol. Failed/missing cells make qualified full-pool score
unavailable; raw complete matrices remain reportable. Printed density residuals
are separate limitations, not silently relabeled as converged forces.

Use only the designated25 calibration sources and the existing extrema/min-gap
rule to define a separate reference, if its required complete stable panel
permits it. Crystals and four folds never contribute to fitting. Mathematical
and operational variants remain separate. Show old-reference transfer separately
from new-protocol decisions; no posthoc threshold adjustment. These are consumed
development sources, not new independent biological validation. Retain all32
sources and all384 cells, choices, failures and numerical gates.

## Execution scope to be prepared

Maximum768 logical continuation calls before exact reuse and missing seeds;
zero new MACE, DFT or optimization. Proposed ordinary allocation is32CPUs/64GiB,
32 concurrent one-rank scalar tasks, CPU-only existing explicit GPU-partition
host, two dependent finite stages. Exact call count and actual seed inventory
must be recorded before submission. No submission belongs to this preparation
step. Existing one-rank anomaly calls averaged5.84 engine-seconds; a full panel
is a minutes-scale CPU experiment with varying context sizes, to be measured
separately from reused historical costs. No new default or mandatory production
qualification gate is introduced by this bounded experiment.
