# Motion-envelope preparation result

The fixed4.3 Å rule prepares every declared pilot input and preserves prior
three-fold preparation coverage: **34/34 pilot pools,135/135 unique source/context
pairs,94/100 complete triples and six prior unavailable triples**. No missing
member was replaced. The100-triple molecular panel remains outside current scope.

Relative to the previous3.5 Å rule, the sole selection change is the polar radius.
Original donor anchors replay in101/101 source discoveries. Complete source-backed
fragments, peptide neighbors, H/protonation, waters, disulfides and cap construction
retain their existing rules. Union membership is frozen over each exact declared
three-source group before search. The25 canonical inputs use their previously
chosen first-complete triple; the three crystals use singleton selections.

Actual contexts contain169–218 atoms (median192). A8 canonical and the two probe
contexts contain192 atoms and include Ser352 through the common rule, with no
A8-specific exception. This probes context/composition/cavity robustness; prior
contact inspection did not establish a new severe clash or a unique hydrogen-bond
mechanism. The reachability bound concerns original moving anchors against omitted
fixed atoms only.

All135 paired physical maps preserve the original core modes and source graph.
Independent read-only review found maximum saved Jacobian error7.85e−9 Å/unit and
bond error1.44e−14 Å. Its one missing-handling correction was applied before
execution: a pilot now also requires every defining triple member and a usable
state anchor. No actual input changed. Version1 geometry products remain immutable;
version2 reconciles their availability ledger without any geometry rerun.

The exact archive audit finds **zero reusable pilot origin MACE states and zero
complete reusable pools**. The approved molecular scope is therefore68 real MACE
origins,68 bounded searches, at most68 cross-MACE calls,136 strict solvent-origin
cells and at most272 strict candidate cells. No historical loose scalar value
substitutes for the separately qualified strict profile. Source preparation took
73.1082 s of local wall time with four workers; it made zero molecular calls.

Artifacts:

- `workspaces/motion_envelope_20260923/run_v2/SELECTION.json`
- `workspaces/motion_envelope_20260923/run_v2/PREPARATION.json`
- `workspaces/motion_envelope_20260923/INVENTORY_v1.json`
- `workspaces/motion_envelope_20260923/pilot34_origins_v1/NATIVE_COLLECTION_v1.json`

Six preparation tests and four execution-preflight tests pass on actual artifacts.
The latter include an actual qualified strict-native output and a deliberately
corrupted copy of a real task. Final classification and costs are reported
separately after the finite jobs finish. Production/defaults remain unchanged.
