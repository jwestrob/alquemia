# Explicit three-source execution adapter: preflight only

Root authorized implementation after reviewing the handoff in commit7ac4723:
"implement the thin execution adapter for that exact six-source handoff,
preserving frozen preparation and reference. Molecular launch still waits for
the full triple-transfer verdict; no need to wait to finish the reviewable
execution code and real-artifact preflight."

The exact physical scope and deferred molecular experiment are frozen in
../pqq_three_source_20260923/EXECUTOR_HANDOFF.md and EXECUTOR_INPUTS.json.
This delivery implements the existing warm origin/search/cross/scalar execution
path for explicit prepared triples, with no A0A3 or historical canonical gate.
The two prepared PLM groups are the only intended prospective integration here;
labels remain unknown. No molecular call is authorized by this preflight plan.

Preflight may create the exact six-source envelope, 12 native origin tasks and
24 original-coordinate vacuum/ALPB input files and run existing integrity/dry-run
checks. It must not submit or execute them. Candidate proposal coordinates,
12 opposite-metal MACE cells and at most48 additional GFN2 cells can only be
materialized after actual search receipts in the later authorized run.

Use frozen threefold reference e725ea83…, precision ftol1e-8, shared four-mode
force selector, SLSQP200/±0.8rad/0.8Å and exact nativeGFN2 MaxIter500/rank1 profile.
No reference/threshold fitting, source substitution, protonation or default change.
Failures remain unavailable and strict median-three requires all three sources.
Completed states/energies may be reused only within the same declared run;
existing source preparation is explicitly reused, archived molecular results are
not. The first usable launch requires a separate durable root agreement after
transfer qualification; this document records preparation/code scope only.
