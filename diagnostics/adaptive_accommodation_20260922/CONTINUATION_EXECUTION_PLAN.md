# Continue the fixed adaptive rule across original references

After the reviewed four-source pilot, root proceeds with the already prepared
remaining26 sources under Jacob's September22 contained development authorization.
Both pilot controls retain their old-band classifications. The original primary
pilot is3/4available; separately qualified MaxIter500 completion gives4/4, with
the failure and its added cost preserved. No classification improvement is claimed.

Use exactly the52 tasks in `remaining26_proposals_v1/manifest.json`, SHA256
`3858cb83e329383212c5f46299c39e634b795ba25cf0765ff5e08a18a01f21b0`.
This is all25 designated canonical training sources plus1KB0, with the other two
crystals and both PLM sources reused from the completed pilot. Keep the frozen
four common angular coordinates selected from the original physical forces,
0.8Angstrom final constraint,0.8radian bounds, original SLSQP settings and one
original start per metal. No new starts, modes, waters, protonation, folding orDFT.

For each source, extend its actual completed original common pool with the two
admitted adaptive geometries. Reuse old cells and own adaptive MACE values;
cross-score both metals over all unique candidates with exact native GFN2 solvent
endpoints. Maximum fresh pool work:52MACE and208GFN2 singlepoints, before deduplication
or unavailable proposals. Retain every source and failure. A failed required
proposal/cell yields an unavailable adaptive score, with the old score separate.

**Numerical iteration policy, fixed before these new endpoints:** allow up to500
native GFN2 SCF iterations, keeping the primary Hamiltonian, smearing, specialxTB
mixer, guess and convergence tolerances unchanged. Apply this ceiling to every
new vacuum andALPB cell in this continuation. This prevents the observed125-cycle
cutoff from needlessly losing a point that converges at129. Previously converged
125-ceiling cells remain compatible; their stopping criteria were identical.
The three-call qualification, including two control replays within0.05kcal/mol,
is pinned explicitly. No restart orbitals or alternate solver recipe is introduced.
An endpoint failing500 remains failed; there is no automatic escalation.

This numerical policy is recorded separately from the geometry-score protocol
and contributes to cache/manifests. The original primarypilot collection and
its failure remain unchanged. Both original baseline scorers retain their existing
inputs/defaults. Qualification is numerical, not predictive validation.

Report original, earlier-proposal, shared-pool and adaptive contrasts; explicit
old-band transfer; candidate works/choices; coverage and measured incremental
cost. A distinct adaptive reference may use exactly the25 designated canonical
training sources and the unchanged extrema/minimum-gap rule. Crystals, PLM
predictions and noncanonical fold outputs cannot set its thresholds. Missing
training members leave calibration unavailable. Freeze any valid reference
before an adaptive fold-transfer experiment. No broader adaptive campaign is
launched by this continuation.

One warmH200/32CPU/200000MiB proposal worker and existing64CPU8x8 solvent shards
use finite manifests. CPU-only work may use either available GPU-host's CPUs;
record the actual host and allocation. No production promotion or PLM cohort
rescore. Root owns pool integration/launch; comparison code and bookkeeping
can proceed independently without duplicating molecular evaluations.
