# Uniform precision: frozen-reference transfer to all 225 folds

Approved by Jacob's standing overnight instruction and root's September 23
explicit authorization after the completed rank28 comparison. This is a
separately calibrated candidate, not an equivalence replacement: the precision34
experiment retained all calls but failed some strict native-GFN2 numerical gates.
Those results and the original full225 ledger remain unchanged and visible.

Keep the exact original 225 source rows: 208 physically prepared, 17 original
preparation exclusions. Reuse six exact completed precision pools (the four
noncanonical common8 sources and the two recovered optimizer-limit failures).
The repaired-H MMOL1770 supplement remains separate; its original exclusion is
not retrospectively removed. No labels, geometry, protonation, water, chemistry,
union membership, source grouping, or q0 results change.

202 fresh sources, in original order split by index modulo four, require
51/51/50/50 sources: 404 bounded searches, at most404 cross-MACE evaluations and
1616 native-GFN2 cells. Every q0 energy/force/solvent cell is reused with its
actual eight-rank receipt. No new q0, DFT, alternate starts, retries, or numerical
variants. Same four common angular modes, paired normalized-force selector,
one q0 start, 200 iterations, +/-0.8 radians and final0.8 Angstrom heavy-atom
bound with existing1e-7 Angstrom tolerance. Only the already tested SLSQP
ftol1e-8 Hartree-equivalent policy differs from the original optimizer.

Use the frozen canonical25-only reference
`workspaces/slsqp_precision_expansion_20260923/REFERENCE_v1.json`, SHA256
`1f8470bbdf7a056098ca261aa10b72bdc940cf71ab860fbbc54090bda1e73250`.
No refit, edge epsilon, band adjustment, or production promotion. Common pool is
q0 plus both new endpoint proposals, evaluated under both metals; operational
and mathematical selections remain separate. Failed required proposals/cells
remain unavailable, with no baseline fallback.

Each warm GPU shard requests one H200,32 CPUs and200000 MiB. Each dependent
native solver shard requests32 CPUs/64 GiB with32 independent one-rank workers,
using the existing runner. This scalar execution profile is qualified by the
actual336-cell rank28 comparison in
`workspaces/native_gfn2_rank_panel_20260923/run_v2/COMPARISON.json`;
it passed every0.1 kcal/mol component and0.2 kcal/mol pooled-contrast gate.
Its strict canonical threshold-edge abstention is retained, not patched.
Native recipe remains GFΝ2, electronic300K, ALPB water versus vacuum,
MaxIter500, unchanged mixer/convergence/NoAutostart. Rank count is an execution
setting, not a changed Hamiltonian. Existing eight-rank caches stay labeled.

Report all225 outcomes, strict75 source-group summaries and100 correlated La
triples using the established complete-member rules. Compare with unchanged
released/DFT/native/adaptive/standalone ledgers on full and common coverage.
Report raw contrasts, choices, source-conditioning, failures, boundary flags,
actual coordinate changes and matched numerical gates (.001 kcal native
proposal, .1 kcal medium cell, .2 kcal pooled R), without treating those gates
as proof of biological accuracy. Biological labels do not select inputs or
geometries. Count actual fresh molecular calls, cached q0 evaluations, wall time
and requested allocations separately. No automatic rescue or follow-on sweep.
