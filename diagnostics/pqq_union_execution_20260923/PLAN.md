# Matched fresh source-to-score integration — approved preparation, launch pending

2026-09-23. Root, acting within Jacob's explicit overnight discriminator scope,
approved the following concrete comparison and implementation: all ten existing
A0A3F2YLY8 reference folds, released static followed by union/adaptive with the
new stopping precision and one-rank scalar profile. Molecular submission awaits
the full225 result or root's confirmation of completed preflight. This file is
not permission to submit before that coordination checkpoint.

## Inputs and method

Use exactly the five Ca-conditioned and five La-conditioned source structures
for `a0a3f2yly8-pqq-la_model` in the declared all250 inventory. Preserve raw source
pins, selectors, actual archived canonical membership, assembly, protonation,
cofactor and water policies. No source is selected by energy or outcome. These
are consumed structural repeats of one biological reference, not ten independent
proteins or new blind validation.

Two sequential jobs on the same permitted H200 host, in the fixed order below.
Each requests 32 CPUs, one H200 and 200000 MiB host memory. Each independently
runs the existing fresh source preparation (including its explicit Ca-residue
alias adapter) in a separate output directory. Preparation/normalization failures
remain visible; never replace fresh structures or energies with archived values.

1. **Released static:** current local complete-context preparation and the
   released native MACE + GFN2 ALPB−vacuum score/reference, unchanged. Twenty
   fresh MACE scalar endpoints and 40 native GFN2 cells, four eight-rank workers,
   original default125 inputs. Use the existing released scorer, including its
   per-source warm two-endpoint batches.
2. **Explicit candidate:** fixed context membership is the union over all ten
   declared sources, with each source retaining its own coordinates. Same paired
   physical four-mode selector and native MACE objective; SLSQP maxiter200,
   ftol1e−8 Hartree-equivalent units, ±0.8 rad and 0.8 Å displacement with the
   established numerical tolerance. Twenty fresh q0 MACE gradients, twenty
   bounded optimization starts, at most twenty cross-metal MACE evaluations,
   and 120 native GFN2 cells (40 q0 + 80 new candidates), MaxIter500, one rank
   each and at most32 workers. Common pool: origin/adaptive_Ca/adaptive_La,
   both metals on every admitted geometry, unchanged energy-only selection.

Total fixed fresh work: 160 GFN2 calls, 60 non-search MACE evaluations and twenty
bounded native MACE searches. Count actual search evaluations and all failures.
Zero DFT, standalone xTB, new fold, scientific retry or alternate search.
Fresh q0 gradients and each optimizer's own final energy are reused only inside
their own candidate execution. No archived molecular energy/force supplies a
fresh cell in either arm. Archive use is limited to qualification, fixed
references, source identity and comparison after execution.

## Numerical identity and delivery

Candidate inputs pin the completed canonical-only precision reference and the
336-cell one-rank qualification; the old union replay profile remains accessible.
No source-specific threshold, source selection, calibration or default change.
Keep exact-band effects separate from scalar numerical equivalence.

Add an isolated execution adapter alongside `pqq_union_candidate.py`; reuse its
request/preparation checks and existing source/ORCA/MACE/optimizer kernels. Keep
its immutable replay API untouched. Existing manifest/cache identities include
source/prepared coordinates, group union, microstate, caps, charge/spin, model and
software, optimizer settings, GFN2 input/binary, explicit rank profile and frozen
reference. A cache hit for old settings cannot claim a new-profile execution.
Partial files and failures remain visible; no automatic scientific rescue.

Report all ten rows and strict declared structural summaries; original local
static, union origin and accommodated scores remain separate. Retain selections,
works, boundaries, unsupported modes/preparations and raw component values.
No affinity probability, free-energy or stationary-minimum claim.

Measure fresh preparation, model startup, scalar solver, search/cross scoring,
collection and full allocation separately, including per-source receipts where
the existing runners provide them. The released path loads one warm MACE batch
per source; the adaptive path uses one worker per stage. End-to-end timing thus
includes different batching and model loads. Do not attribute any difference
solely to rank count/accommodation or claim superiority over an equivalently
optimized static implementation. This experiment has no third arm.
