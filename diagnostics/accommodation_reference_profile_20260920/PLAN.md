# Geometry-selected known-reference extra-Asp response

Root authorized this contained follow-on under Jacob's overnight goal. Selection
was fixed by the earlier, score-independent <2.2 Å inventory: the sole flagged
extra-Asp reference fold is `mmol_1770-pqq-la_model__conditioned_Ca__seed-1_sample-1`.
Root had already inspected its native MACE misclassification; this is consumed
development, not blind validation or an independently selected test case.

Use its exact prepared complete polar context, source Ca-conditioned geometry,
original charges/H/PQQ/water inventory and verified physical terminal extra-Asp
CB–CG torsion. Angles are −0.2, 0, +0.2 rad for both Ca/La electronic endpoints.
No ion moves, minimization, Glu alternative or preparation change. Four new native
float64 OMOL scalar evaluations and eight native GFN2 vacuum/ALPB(Water)
singlepoints cover the displaced states. Reuse both q0 OMOL receipts and all four
successful q0 GFN2 receipts from the root fold campaign's executed shard_2;
verify exact coordinates/charges/method and preserve source manifest/task IDs.
No new DFT evaluation is authorized or needed for this pilot.

Report all three fixed-angle contrasts for native OMOL and the established
OMOL+[GFN2 ALPB−vacuum] composite. For each model separately, predeclare the
bounded-grid lower-energy descriptor

`R_grid = min_q E_Ca(q) − min_q E_La(q)`, q∈{−0.2,0,+0.2}.

Require every point in the two-metal grid to succeed; a missing point makes the
corresponding descriptor unavailable. No favorable subset or replacement score.
Report selected angles and per-metal lowering relative to q0. This is an
explicit local accommodation descriptor, not a free energy, entropy estimate or
validated relaxed structure. All source-composition and coordinate measures match.

Apply the already released context bands only as an explicitly labeled transfer
test, side by side with q0. No threshold fitting, inherited calibration claim,
production change or broad accuracy conclusion. A source Ca structure crossing
the La-oriented warning threshold is not automatically defective. This tests
whether this exact physical perturbation improves a consumed reference result;
its energy-surface qualification is separate and currently pending native DFT.
