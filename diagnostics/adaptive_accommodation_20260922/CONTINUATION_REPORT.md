# Remaining 26 contexts prepared, no optimization launched

The unchanged four-angular proposal method can now prepare **all 25 canonical
PQQ references plus 1KB0**, excluding exactly the four already executed pilot
sources. This supplies the next fixed reference panel; preparation does not
establish improved discrimination.

Manifest:
`workspaces/adaptive_accommodation_20260922/remaining26_proposals_v1/manifest.json`

SHA256: `3858cb83e329383212c5f46299c39e634b795ba25cf0765ff5e08a18a01f21b0`

**26 cases / 52 endpoint tasks pass dry-run.** The real prelaunch collection has
zero available candidates, with all new solvent/composite/score fields null.
These are unrun tasks, not scientific failures. No molecular calls, optimizations
or cluster submissions were performed. Root retains the execution decision after
the separately owned numerical check.

## Focused changes

`adaptive_angular_proposals.py prepare` accepts
`--population pilot4|remaining26`. The default remains `pilot4`; old commands and
the original `CASES` import remain compatible. Remaining membership is the exact
original-30 source order minus the fixed four pilot IDs. No outcome, class label
or convergence status enters that choice. Validation, execution return values and
collection use the declared population's actual denominators.

The protocol ID, `SETTINGS`, model, force selector, physical maps, constraints,
optimizer and one-q0-start rule are unchanged. Source comparison confirms the
optimizer and physical-coordinate functions are identical to the original
snapshot. The original `b5c4218...` manifest, implementation snapshot, inputs and
eight actual candidate results remain intact. Repeating the default preparation
reproduces **every original manifest field except the new implementation-copy
paths and hashes**; task state, selection, scope and settings are identical.

The execution gate still requires the completed **original four-case common
pool**, with the same source and model. Its actual completed collection passes;
the real incomplete snapshot is rejected. The adaptive-expanded pool is a
different protocol and is not substituted for the gate.

## Real checks and limits

The combined suite ran **15 tests: 14 passed, one skipped**, in 60.850 seconds
([final log](CONTINUATION_TESTS_v2.txt)). All eight new continuation tests passed.
The inherited skip is the original pilot's prelaunch-only collection test: those
eight optimizations have already completed. The retained real unrun 52-task
collection is explicitly checked by the new suite and remains a stable fixture
after any later launch.

Checks cover all remaining paired maps, retained source coordinates, small
physical motions, finite-difference verification of the analytic constraint
Jacobian on fixed first/middle/last sources, default-pilot replay, unchanged
physical/optimizer functions, actual pool gating and corrupted scope/count copies.
Finite differences evaluate geometry only; no scientific energy output is mocked.

The first test run exposed a test metadata assumption: 1KB0 carries the same
`canonical_PQQ_functional_class` scope as the model references. The corrected
scope test uses the frozen source IDs to identify 25 model inputs plus that
crystal; no source label, task, geometry, selector or setting changed. The first
test log is preserved alongside the passing run.

No new calibration, GFN2, DFT, composite optimization, score selection or default
change belongs to this adapter. The later common-pool evaluation remains owned
by root. [Commands](CONTINUATION_COMMANDS.md) validate the exact snapshot and show
the existing allocation wrapper for that later decision. [The plan](CONTINUATION_PREPARATION_PLAN.md)
records this preparation-only scope; the vault note records the handoff.
