# Canonical28 plus six probes: strict-native envelope pilot

Parent authorized this exact finite pilot after135/135 source/context and paired
mapping preparations,34/34 pilot inputs, unchanged94/100 primary preparation
coverage and an exact archive audit. No pilot origin or complete pool can be
reused. Original100 triple transfer remains unlaunched. Jacob's standing autonomy
applies; this records the consequential scalar profile before its calls.

Population and4.3 Å membership are exactly PLAN.md and run_v2/SELECTION.json.
Only25 designated canonical geometries fit the new class-extrema/minimum-gap
reference. Three singleton crystals and six prescribed probes are excluded.
No source/state/water/protonation, physical selector, optimizer or bounds change.

68 native OMOL origin energy/force evaluations ran as1211294 using the existing
warm H200 runner,32CPU/200000MiB. Actual complete native origins feed68 one-start
four-mode searches (SLSQP200,ftol1e−8 Hartree-equivalent,±0.8rad/0.8Å). Both metals
score the same origin/Ca-proposal/La-proposal pool, at most68 new cross-MACE calls.
Failed endpoints remain visible; no alternate starts or adaptive rescue.

**New separate scalar numerical profile:**
`Nikasha_native_GFN2_ALPB_vacuum_TolE1e10_fresh_MaxIter500_rank1_v1`.
Use the exact qualified `strict_native_pool.recipe(charge, medium, 'fresh')`:
ORCA6.1.1 native GFN2,UseXTBMixer true,300K,MaxIter500,TolE1e−10,NoAutostart,
scalar single point,one rank. All other Hamiltonian/state controls unchanged.
Qualification is the completed32-source fresh-versus-cold-seed comparison:
384/384 component agreements and both branches32/32 correct with their own
canonical references. It does not make old loose energies interchangeable.

New scalar scope is exactly136 origin cells plus at most272 candidate cells,
32workers×one rank in32CPU/64GiB allocations. No loose scalar reuse. Every accepted
cell must have actual native recipe/state, printed TolE1e−10, scalar energy,
normal completion, charge audit and one-rank receipt. Missing solvent components
remain unavailable, never zero. Retain unrounded energies and solver diagnostics.

Native-vacuum searches do not require solvent gradients. Their preparation may
precede scalar completion using actual native origins only; no invented composite
q0 is permitted. Final pools require actual complete scalar components and the
same frozen candidate-selection policy. Compare against strict-native existing
contexts wherever exact sources match; parent owns the four missing old-context
probe calculations. Do not attribute a mixed numerical/context change to context.

No default promotion, PLM rescore, newDFT or full100 molecular transfer. Report raw
components, source-pair spread, failed/boundary candidates, canonical gap and all
six probe outcomes. Incomplete or overlapping canonical calibration blocks transfer
expansion. Actual scheduler receipts cover all allocations without nested-cost
addition; tests and vault record the final outcome.
