# Multisite AMOEBA framework preparation capability

Declared before new parameterization, during frozen GGR transfer1201162.
Authorized by Jacob's active autonomous MACE goal. This independent preparation
work adds no energy evaluation, DFT, MACE inference, force, relaxation or score.

Question: can the installed, pinned AMOEBA2018 protein/water/calcium templates
represent the five unchanged multisite physical preparations already used by
MACE? Use all five `multisite_prepared_v2` cases: PARV_4CPV_[CD,EF] and
AEQ_1SL8_[EF1,EF3,EF4]. They are consumed cases, not new blinded labels.

Five framework parameterizations using the existing OpenMM capability helper,
with a new explicit opt-in for real background calcium. Keep selected QM metal
in the physical ledger and omit it only from this parameter-only framework,
exactly as in the existing single-metal path. Retain every other Ca as the
installed AMOEBA2018 CA template (type358), including its native charge,
polarizability and GK parameters. No La force-field guess. The selected metal
will later receive the frozen common physical boundary only in a declared
full hybrid preparation, which is not implemented by this capability test.

Retain all real protein atoms/coordinates, the source ACE0–ALA1 connection in
4CPV, archived protonation, and the same union of explicit waters (4CPV166;
1SL8 716/698/680). Preserve archived H normalization. No new atoms, dropped
ions, terminal completion, protonation change or inferred covalent metal bond.
Verify actual source and paired endpoint inventories, graph, templates, full
framework charge = protein charge +2 per background Ca (waters neutral),
and positive supported radii/polarizabilities. Record all failures explicitly.

Existing helpers default to the old single-metal behavior. New preparation
protocol identifies the multisite opt-in. No cavity or charge rule changes to
executed GGR/alpha studies. This capability result does not validate a hybrid
score or supply missing core/source/density components.

Five local CPU preparation calls, one process, no GPU or cluster allocation.
Recent AMOEBA preparations take seconds to minutes and modest host memory;
measure actual wall/CPU/RSS including failed attempts. All products under
workspaces/mace_omol_20260917/multisite_amoeba_capability_v1. New quantum/short/
solver scoring requires a separate declared manifest after source-core and
background accounting are implemented, irrespective of this capability result.

Evidence stays separate: parvalbumin supporting cross-study La direction;
aequorin ordered EF1/EF3/EF4 without site labels. No classification fitted here.
