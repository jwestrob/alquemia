# Preserved DFT comparator for the existing 100 three-fold descriptors

Recorded2026-09-20 before applying this comparator. Root explicitly requested
the addition under Jacob's overnight accuracy goal while416 DFT endpoints run.
No new molecular call, source preparation, fitting or altered running snapshot.

Use the **exact100 triples** already declared in
`diagnostics/accommodation_goal_20260920/THREE_FOLD_PLAN.md` and saved in
`workspaces/accommodation_goal_20260920/folds_v1/triples_v1/result.json`:
every three-of-four subset of each protein's four noncanonical La-conditioned
folds, all25 reference proteins, all four subsets per protein. Core/context
MACE rows must describe identical membership. Do not select a favorable subset.

For DFT, read a separately named immutable collection snapshot from the running
baseline experiment. Verify available source endpoint receipts and raw contrast
algebra. Apply the **original DFT bands** from its authoritative released
reference to the ordinary median of all three raw R values. Any absent source
pair makes that triple unavailable; retain actual missing members and statuses.
No new DFT threshold, substituted MACE value, zero correction or successful-only
median. In-progress endpoints remain unavailable in partial snapshots.

Report baseline DFT beside the four **existing, unchanged** MACE arms:
native core, native context, core+solvent, context+solvent. Retain each method's
own bands, all100 triples, and all25 biological groups. Report every median,
decision, outcome and missing member, plus correct/wrong/inconclusive/unavailable
counts. Per-protein flags use the existing ordering: wrong, unavailable,
inconclusive, correct; worst-scored outcome excludes missing coverage, while
`all_four_correct` requires all four triples available and correct.

This is a separate developmental comparator addendum. Original La4/Ca5/balanced
comparisons and the completed MACE triple report remain immutable. Partial
completion depends on execution order and cannot establish overall superiority.
The100 overlapping subsets are not independent biological observations. No
three-fold PLM validation, promotion or production rescore is implied.
