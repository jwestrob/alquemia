# One-rank scalar qualification across the fixed union reference panel

2026-09-23. Jacob's standing overnight authorization applies. Root first requested
preparation only, then explicitly approved execution of “all28 canonical/crystal
original union pools,336 fresh one-rank scalar cells,32 workers/32CPU/64GiB CPU-only,
exact source/input electronics incl original125 vs500 MaxIter preserved.”

The completed matched rank pilot found all24 cells converged and rank-dependent
energy differences below1.3e-9 kcal/mol, with lower measured one-rank cost. This
panel asks whether that scalar equivalence extends over the actual reference
chemistry and three fixed candidate geometries. It does not qualify native forces.

Membership is exactly the28 rows of the immutable union/adaptive canonical
reference (SHA256 2f142883dcfd8b452d6c75e714b49ee1cac7a29ae317a4f289dcbc740498c5c7):
25 already consumed calibration proteins and3 consumed crystal controls. Each
uses origin,adaptive_Ca,adaptive_La; both metals and vacuum/ALPB:336 native GFN2
single points. Contexts contain145–202 atoms. No new geometry, MACE, DFT,
protonation, waters, optimizer, biological comparison or reference fitting.

Copy each actual executed input and XYZ byte-for-byte. Preserve ORCA6.1.1,
native mixer,300K,NoAutostart,maxcore2000,charge/spin and exported parameters.
284 input files explicitly use MaxIter500;52 retain their original default125.
Only runtime PAL changes from8 to1. Use the existing no-binding MPI/thread fixes.
All336 comparators actually ran with8 ranks. None of the eight completed pilot
one-rank cells matches the panel's exact input+XYZ+state hashes, so reuse is zero.
Keep actual archive pins and this negative reuse finding; do not substitute a
nearby geometry or select a favorable rank solution.

One CPU-sharing job:32 one-rank workers,32CPUs,64GiB, no GPU, on an existing
permitted GPU-partition host. No custom wall/CPU cutoff. Keep the running
precision expansion's8-rank settings and jobs untouched. This is a different
batch/concurrency from the rank pilot; no matched-hardware speed claim follows.

Frozen acceptance: every vacuum/ALPB scalar cell must differ by at most0.1
kcal/mol from its exact eight-rank source; each mathematical and operational
shared-pool Ca-minus-La contrast must differ by at most0.2 kcal/mol. Report also
all84 fixed-geometry contrasts and endpoint transfer components. Component
failures cannot be excused by cancellation in a pool contrast. All required
cells must converge with matching state and parameter exports; otherwise the
affected pool is unavailable. No automatic retry or fallback to eight-rank data.

Reuse each actual MACE energy unchanged. Reapply the same three-candidate pool
selection (including the0.1kcal origin-retention policy) and the existing frozen
union reference. Retain both old/new selections, works, raw contrasts and calls.
Near an exact calibration extremum, tiny floating-point drift may alter a strict
band call; report it rather than changing bands. Numerical qualification and
classification replay are distinct reported outcomes. No default promotion.

Preserve all outputs/receipts and actual full sacct job+step accounting. The
current finite manifest will be validated before submission. Collection must
still run if an endpoint fails. No additional cell, rank setting or scientific
retry belongs to this experiment.
