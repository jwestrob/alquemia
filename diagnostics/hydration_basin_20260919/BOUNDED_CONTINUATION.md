# Bounded recentering, declared before new native validation outputs

StageA cheap results show several minima at the translation trust boundary.
They cannot supply harmonic entropy. Jacob authorized the full coupled-motion/
occupancy continuation. Keep the16 originally declared soft/response direction
checks. Add one native proposal endpoint per pilot center (four total), whether
interior or boundary. Mark a boundary endpoint `proposal`, never `minimum`.
This changes the StageB finite inventory to20DFT and20matched cheap evaluations;
no result has yet been calculated at any of these new validation geometries.

These four actual native gradients can support at most two successive recentering
rounds on the same four states if the local energy/force checks support using the
cheap model. Each new local step retains the original0.20Å/0.35rad trust region;
no radius or acceptance tolerance is increased. Rebuild the Cartesian anchor
from real DFT and MACE gradients at the new point. Record each protocol generation,
all steps, failures, original coordinates, total displacement and metal–O distances.
Each additional round contains four cheap local response tasks and up to four
native EnGrad endpoints, in a fresh manifest. Stopping at a boundary or failing
force consistency keeps free-energy output unavailable, rather than clamping modes.
No biological classification is used to choose steps, centers or stopping rules.

The purpose is to locate physically supported local stationary states for the
basin calculation, not to erase the original trust-boundary failures. This is
bounded surrogate-guided response research, not an open-ended quantum optimization.
