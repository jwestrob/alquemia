# Approved supplemental MMOL1770 La4 scoring

Parent records Jacob's standing authorization and explicitly approved this exact
experiment on September23: archived repaired H, unchanged heavy atoms/state and
frozen existing union membership; released-local static plus union static and
adaptive. Historical225 failures/results remain untouched. This is one consumed
structural replicate, not a new biological observation.

Input: prepared_v1/PREPARATION.json SHA256
5b3f68031e97c7d669b2389636dd5131b2172ee4c6dc13684359e08922cdcade.
Local160 and union184 atoms, paired Ca−3/La−2 singlets. Preserve all coordinates,
waters, pH7 protonation, cap policy and the archived same-potential H repair.
The preparation PLAN and old failure remain immutable; this separate agreement
authorizes molecular execution after the successful source compatibility check.

Use native OMOL energies/analytic forces plus native ORCA GFN2 ALPB−vacuum,
identical to original union work: MaxIter500, electronic300K, original mixer,
thresholds, solvent and model. No standalone xTB, restart, new precision policy,
DFT or reoptimization of hydrogen preparation. Freeze software/executable/input
pins in finite manifests. First four q0 MACE endpoints and eight GFN2 endpoints
score both contexts. Preserve failures; unavailable union q0 blocks its search.

For the union context only, reuse its actual paired q0 forces to select the same
four physical angular modes by the existing normalized individual/differential
load rule. One original q0 start per metal, original SLSQP Hartree scaling,
ftol3.6749322179347735e−11 and maxiter200, ±0.8rad angles and final0.8Å heavy
displacement (existing1e−7Å numerical tolerance), identical chemistry guards.
Intermediate infeasible geometry and every failed attempt stay recorded.
Do not import the separately tested1e−8 stopping tolerance. No extra start/retry.

Score the common three-candidate pool (q0, Ca proposal, La proposal) under both
metals. Reuse both actual own-proposal MACE results and q0; at most two fresh
cross-MACE and eight GFN2 endpoints. Total scope: six fixed MACE calls plus two
finite searches and16 GFN2 calls, zero DFT. Missing proposals yield missing
adaptive score, never substituted baseline success. Same0.1kcal origin-selection
policy; report mathematical and operational minima separately.

Apply frozen released-local, union-static and original union-adaptive reference
bands only as explicitly identified supplemental preparation-transfer checks.
Do not recalibrate or merge this result into historical225. Report raw contrasts,
components, selected geometry, boundary flags, all failures and exact allocation.
This tests whether a verified preparation defect excluded an otherwise usable
source, not whether hydrogen coordinates are physically optimal.

Existing warm1H200/32CPU/200000MiB runner and CPU64/128GiB eight8-rank solver
layout; independent q0 stages then search/cross stages. Expected seconds-to-minutes
of molecular work based on existing160–200atom union receipts; queue/allocation,
validation and loading overhead reported separately. Finite search limits above
are the original scientific protocol, not a new global compute budget.
