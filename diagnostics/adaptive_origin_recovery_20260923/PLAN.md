# Approved direct-origin adaptive recovery of two inherited failures

Jacob's 2026-09-23 overnight instruction authorizes continued contained
experiments and parallel agents; durable source:
`../overnight_discrimination_20260923/CURRENT.md`. Root explicitly assigned this
finite follow-up after its frozen archive replay showed the three-candidate
pool preserves all 204 available full-adaptive calls and canonical-only bands.

Recover exactly these consumed structural repeats:

- `p12293-pqq-la_model__conditioned_Ca__seed-1_sample-4`
- `q60ar6-pqq-la_model__conditioned_Ca__seed-1_sample-1`

Both have valid origin forces, full context state/mappings and vacuum/ALPB
energies, but failed older terminal-only proposals excluded them from adaptive
search. Remove that dependency. Do not rerun the distinct Q60AR6 La-sample0
adaptive iteration-limit failure. Do not alter any source geometry, chemistry,
water inventory, protonation, model or original output.

## Frozen method before new outputs

Use the exact existing common four-angular-mode selector from actual paired
q0 MACE forces. P12293 selects A/209/chi1, chi3, chi2 and A/335/chi2; Q60AR6
selects A/205/chi1, chi3, chi2 and A/331/chi1. Both metals share the selected
source-connected modes. Reuse source-pair preparation and the existing scaled
SLSQP optimizer: 200 iterations, ±0.8 radians, final physical heavy displacement
≤0.8 Å, the same objective/gradient/ftol scale and geometry guards. One q0 start
per metal/source, **four optimization starts total**. Failure stays unavailable;
no alternative starts, clipping, relaxed gates or origin-only fallback.

For every successful paired search, score the same three geometries for both
metals: origin, adaptive_Ca, adaptive_La. Reuse exact archived origins and the
native MACE values already computed for each candidate's generating metal.
At most **four cross-metal native MACE evaluations and 16 new native GFN2
single points**. Vacuum and ALPB(water), original primary native solver/300 K,
MaxIter500 qualification, NoAutostart; do not introduce the separate experimental
two-continuation policy into this reference-compatible comparison. No new DFT.

Composite E = MACE_vac + GFN2_ALPB − GFN2_vac; R = E_Ca − E_La. All six
metal/geometry cells must be complete for a source score. Reuse mathematical
row minima and operational 0.1 kcal/mol origin-retention policy. Apply root's
frozen canonical-only `workspaces/adaptive_minimal_pool_20260923/REFERENCE_v1.json`
without refitting. Report coverage and decisions in the full 225-source ledger,
keeping the old two unavailable rows beside their new results. These are
consumed Ca-class structural repeats, not new independent biological controls.

## Execution and delivery

New scoped adapter reuses the existing optimizer, worker and ORCA executor;
production/default remains unchanged. GPU: pinned native float64 OMOL, one GPU,
32 CPUs/200000 MiB. Native GFN2: CPU-only gpu partition, 64 CPUs/128 GiB,
eight concurrent eight-rank calls. Finite manifests/preflight precede submission.
Preserve actual calls, failures, receipts, source maps, costs and final results.
No automatic expansion beyond these two rows follows.
