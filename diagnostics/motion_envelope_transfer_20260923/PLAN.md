# Approved finite full100 transfer of the frozen motion envelope

Parent explicitly authorized full100 transfer after the completed canonical28 + six-probe
pilot: "Authorize full100 transfer at that frozen scope once the three exact pool overlaps
verify" (2026-09-23). Scope:104 source/membership pairs,94 complete +6 prior unavailable
triples;3 exact pilot reuses and101 new pools. Maximum new work is202 native origins,
202 searches,202 cross-MACE,404 origin and808 candidate strict GFN2 cells. This inventory
phase itself makes zero molecular calls. Submit the finite stages after preflight.

Keep all 100 original three-of-four noncanonical La triples, including six prior
preparation-unavailable triples. All 94 complete triples use their exact saved
4.3 Å fragment union and original states/maps. No extra probes, changed membership,
waters, protons, folds, DFT or altered cutoff. The separate Ca-conditioned A0A3
sample3 abstention remains a material limitation, regardless of La-triple results.

Use the frozen pilot canonical25 reference REFERENCE_PINNED_v1; no refit or new
label selection. Preserve the own adaptive mathematical and operational variants
and separate origin-only reference. Compare on the exact same triples with released
static, old threefold, tenfold precision and strict tenfold where available, retaining
missing members. Median requires all three declared members. All four triples per
protein are reported; no favorable subset selection.

Unchanged scientific rule: native OMOL q0-force four-mode selector, one search per
metal, SLSQP200 with ftol1e-8 Hartree-equivalent, ±0.8 radians / 0.8 Å admitted heavy
movement; shared {origin, adaptive_Ca, adaptive_La} pool. Both metals score all
members. Native GFN2 uses the qualified fresh NoAutostart / TolE1e-10 / 300K /
MaxIter500 / one-rank vacuum and ALPB recipe. No loose scalar substitution.

Audit exact completed pilot pools against source-row/state/map/checkpoint/settings
and actual native/scalar receipts. Use the already completed all135 physical archive
audit to exclude incompatible old origins/pools; do not regenerate preparations.
Two disjoint proposed warm GPU shards use 32CPU, one H200 and200000MiB each; each
scalar stage uses32CPU/65536MiB,32 workers×one rank. Scope files are not executable
scientific manifests. Preserve every failure and actual cost; no default promotion.
