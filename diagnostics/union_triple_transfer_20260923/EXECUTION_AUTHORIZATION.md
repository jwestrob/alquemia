# Exact full100 transfer authorized after reuse audit

Parent authorization after reviewing the actual finite counts:

> Authorize exact full100 transfer after finite fixture preflight: all94 complete/
> 6 unavailable,70 exact whole-pool reuses+55 new pools,19 origins38 MACE/76 GFN,
> 110 searches+110 crossMACE+440 candidateGFN maximum. Freeze new3fold reference/
> selection before calls; two disjoint28/27 warmGPU shards and rank1CPU stages/
> resources as proposed. No extra sources, refit or threshold changes.

This delegates Jacob's standing authorized discriminator work; no additional
biological population or scientific parameter was added. PLAN.md remains the
unchanged preparation-stage record. `run_v2/AUDIT.json` contains the actual125
pair ledger, accepted70 pool receipts and maximum map-coordinate difference
1.7763568394002505e−15Å. The initial read-only audit stopped at a last-bit cap
coordinate comparison; existing1e−12Å copy equivalence now applies only to numeric
coordinate arrays. All discrete identities, bonds, cap rules, modes and settings
must remain exact, and actual archived maps are retained.

Origin manifests contain exactly38 MACE and76 GFN tasks. A separate finite
MOLECULAR_SCOPE lists110 searches,110 cross-MACE and440 candidate GFN requests;
candidate coordinates will exist only after admitted searches. Thirty-six paired
selectors preflight from actual archived origin forces;19 are explicitly pending
the declared origin calls. No guessed force or dummy result fills those fields.

Two subsequent shards contain28 and27 source pairs, with the same fixed
reference and all100/125 denominators in final collection. All failures remain.
Whole allocation receipts determine measured cost; inherited receipts retain
their actual old hardware/ranks. No production or PLM rollout.
