# Three explicit La-conditioned sources: preparation-only interface

2026-09-23. Root explicitly assigned a bounded implementation-only delivery under
Jacob's overnight discriminator authorization: remove the requirement for a
historical canonical member from the eventual three-source opt-in route.
Production, the original tenfold reference-only API and running transfer remain
unchanged. Molecular execution/general eligibility wait for the transfer verdict.

Accept exactly three distinct, explicitly selected La-conditioned Protenix PQQ
source files, their metal/PQQ/role/assembly selectors and the current pinned source
configuration. Require actual identical sequence/numbering, roles and assembly;
verify actual selected source metal identity. Choose the lexicographically first
source ID as physical-state anchor before preparation or energy. It is not a
biological label, state selection from scores or a calibration member.

Use the existing source preparation and complete-context graph kernel. Fix the
fragment union from all three declared sources before energies. All three must
have the same complete selected protein/atom/cofactor/proton-parent/cap/bond/
formal-charge/spin/water state. Preserve each source's coordinates, paired Ca/La
mapping and original core. A missing source/anchor or unsupported state makes the
three-source preparation unavailable; no smaller union or substitute is selected.

Pin the new frozen three-La-membership reference supplied by second_shell:
workspaces/union_triple_pilot_20260923/REFERENCE_v1.json. This interface may prepare
compatible inputs and report reference identity, but emits no score or molecular
execution command before the separate transfer qualification.

Implement request/prepare/dry-run in scripts/pqq_three_source.py, with own tests
and records. Fixture operations reuse exact existing local preparations for
predeclared A0A3 triple000 and Q9 triple096, then rebuild the same source-backed
union and maps. Retain the real unsupported MMOL1770 triple037 as an eligibility/
missing-input fixture (not a new biological comparison). Zero new protonation,
folds, molecular energies, gradients or optimizations in this delivery. The fresh
preparation API may call the existing protonator when explicitly invoked later.
