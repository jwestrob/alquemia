# Calibration-first staging and singleton controls

Root clarified before any new energy on 22 September: run the 25 canonical union
preparations plus the three existing crystal transfers first. The crystals are
singleton unions of their actual archived fragment list and must reproduce the
old context. Reuse their energies only after coordinate/state/checkpoint/native
input/receipt checks. Keep their three-source denominator separate from the
250-fold union preparation.

Freeze the canonical-only new reference (or its explicit unavailable status)
before launching the 36 declared noncanonical pilot sources. That transfer-stage
manifest requires the reference pin. The calibrated class-extrema rule and all
membership/state rules in PLAN.md remain unchanged. No fold scores are used to
choose membership, determine the reference or repair a preparation.

Matching already-converged native GFN2 results can be reused under the qualified
500-iteration policy: only the iteration ceiling can differ in input text, and
the actual old receipt stays identified as an old result. Identical electronic
state/method and source coordinates within the predeclared 1e-12 Å arithmetic
tolerance are required. New versus reused cells are counted explicitly. A failed
old endpoint is not success and is not relabeled as a new receipt.
