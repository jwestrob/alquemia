# Independent union/adaptive225 review — execution still in progress

Read-only review by water_basins on September23. No molecular calls, scoring
reruns or shared source edits. No blocking issue found in the current selection
or comparison implementation. Final225 numerical outcome is not yet available
and is not validated by this review.

## Concrete checks

- Exact225 primary noncanonical rows are recovered from the original source
  inventory. Four completed consumed-fold pools are reused,204 supported fresh
  sources are split51 per shard, and17 preparation exclusions remain in the
  denominator. Canonical folds are explicitly excluded. No label or source
  substitution occurs.
- Canonical25 reference froze at08:19:45.941049UTC; transfer selection followed
  at08:28:14.949807UTC. Selection validation checks that ordering and hash pins.
  Crystals/folds do not enter calibration. Both current reference variants are
  available and retain their original extrema; no new threshold is calculated
  in the transfer comparator.
- Every prepared common pool requires all candidate/metals' component cells.
  A failed matrix cell stays unavailable. Mathematical and operational choices
  are recomputed separately from actual matrices; the existing0.1kcal origin
  selection rule and each variant's own frozen bands remain distinct.
- Work algebra explicitly checks newR−q0R = workCa−workLa. Source conditioning,
  labels and grouping identities are checked during ledger joins. Native,
  recovered-adaptive, standalone, union-static and DFT comparator methods are
  retained with their original references and coverage.
- All225 source rows,75 grouped rows and100 correlated triples remain declared.
  La4/Ca5/triple summaries use strict member medians. Balanced scores average
  the two complete arm medians. Any missing member propagates unavailability;
  there is no best-fold/triple selection or failed-source baseline substitution.
- Full-denominator counts and pairwise common-coverage transitions are separate.
  Wrong/inconclusive calls, optimizer failure reasons, boundary flags and exact
  source collections remain available per row. Actual classification changes
  still require the terminal collection/report; none are predicted here.

Four current real-fixture tests independently pass in2.522s,zero skips. These
exercise the exact source shards, exclusions/labels, malformed real-selector
rejection and comparator-ledger joins. They do **not** yet exercise a completed
full225 comparison: its molecular collections are still running. The owner was
asked to add/run final actual-fixture checks once terminal, covering225/75/100
counts, math/operational selection, missing-member propagation and explicit new
wrong/inconclusive/optimizer-failure identities. No dummy collection is needed.

One documentation issue found during review belongs to my earlier standalone
report: it called the executed strict medians means. A separate erratum corrects
that prose, preserving original report/output hashes and all numerical results.
See `diagnostics/standalone_xtb_transfer_20260923/GROUPING_ERRATUM.md`.

Review/source pins are in INDEPENDENT_TRANSFER_REVIEW_PINS.json. This note is an
implementation/fixture review, not a final scientific result or promotion.
