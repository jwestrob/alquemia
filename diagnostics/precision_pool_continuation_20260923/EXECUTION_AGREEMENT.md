# Exact uniform32 execution authorized

Root explicitly authorized, before submission:

“Authorize the exact uniform32 continuation after finite preflight:384 cold
cells, six exact completed two-pass reuses,756 new scalar calls, no cold
recomputation. Use two fixed passes with actual restart confirmation and uniform
final-stage reporting; freeze canonical-only calibration rule and physical/
numerical acceptance gates before execution. Same existing CPU runner,
32 one-rank workers/32 CPU/64 GiB, no GPU. Preserve density exceptions, all
component shifts and unavailable states; no retries or favourable-stage
selection.”

The exact methods, population and criteria are those already frozen in PLAN.md.
Actual inventory is INVENTORY_v2.json:384/384 seed pairs, six complete reused
two-pass cells. Inventory v1's missing embedded origin audit was a metadata
lookup error; reading the actual pinned source outputs resolved all128 origin
cells without any molecular call. Preserve both inventories. There is no changed
source, geometry, state, score rule or eligibility criterion.
