# Combine fixed context membership and adaptive physical accommodation

## Authorized finite accuracy pilot

Jacob authorized continued contained improvement experiments overnight on Sept23:
“keep on goin ... launch subagents to do what experiments you like ...”. Parent
assigned this combination explicitly after the separate union and adaptive
branches corrected different consumed reference errors. Production, historical
inputs, scores and calibrations remain unchanged.

Population: exactly the eight source IDs, order and physical sources pinned in
`diagnostics/nikasha_parallel_pilots_20260922/INPUTS.json`: 1H4I,4MAE, canonical
Q9Z4J7/Q88JH5, A0A3F2YLY8 Ca samples1/3, A0ACD6B9F2 Ca4/La4. No new folds,
labels, DFT, protonation, water inventory or biological cohort. Canonical aliases
must map uniquely to the original saved source; all eight stay in the denominator.

Use their completed consistent-context preparations: label-independent union
membership already fixed from saved folds, exact original source coordinates,
complete fragment closure/caps and endpoint states. Controls' unions are also
preserved. This combines preparation/composition/cavity and local response; it
cannot isolate a unique second-shell mechanism. Unsupported mapping is an
explicit unavailable case, not a reason to alter the source/context.

## Same physical search, new matched context

Construct the existing source-connected Kinematics through
`coordination_preparation_context.geometry`. Reuse each exact union's archived
native OMOL q0 energy and forces after model/state/coordinates/ordering checks.
All sixteen force arrays exist; no new q0 force calculation is planned.

Apply the unchanged force-projection selector: four common angular donor modes,
alternating differential and individual projected load with physical norm and
rank checks. Labels and energies do not select modes. Preserve native-vacuum
OMOL, the exact checkpoint, single q0 start for each metal, scaled SLSQP objective
and gradient in Hartree-equivalent units,200iterations,±0.8radian and0.8Å maximum
physical-heavy displacement. Use `adaptive_completion.optimize` and its frozen
settings/physical guards. Do not introduce free cap motion, metal/PQQ/water
translations, FF scaffold energy or the failed solvent-force optimization.

Exactly sixteen endpoint searches (two per source) are permitted. Each final
candidate must pass the unchanged optimizer-success, native nonincrease,
physical bond/cap/overlap and trust-domain checks. Boundary flags and residual
forces remain visible; no claim of an unconstrained/composite minimum. No
additional starts, clipping, rescue or favorable replacement.

## Minimal common pool and unchanged score expression

For each source retain only {origin,adaptive_Ca,adaptive_La}. Both metals must score
that same physical candidate set. Numerical-copy deduplication uses the existing
1e−12Å rule. Require both intended adaptive results; failed candidates/cells make
the primary pool unavailable. Keep the earlier source result separately.

E_M = E_OMOL,vac,M + E_GFN2,ALPBwater,M − E_GFN2,vac,M;
R = E_Ca − E_La. Convert native eV and quantum Hartree exactly once. Reuse the
actual union q0 components and candidate-own native values; at most sixteen
cross-metal MACE calls and64GFN2 single points. Native GFN2 MaxIter500,300K,
NoAutostart/native mixer and original unchanged-convergence policy, not the new
experimental continuation policy. Retain the original MaxIter125/500 receipt
identity where converged q0 values were explicitly reused by the union panel.
No extra contribution, coefficient, aquo reference or entropy is supplied.

Use the established mathematical row minima and separate0.1kcal/mol operational
origin tolerance. No absolute-energy minima across sources or fold ensembles.
Report endpoint/component work, raw R, Ca/La guardrail ordering, prescribed-fold
spread, unavailable cases and explicit transfer through each already frozen
union/released/adaptive band. These transferred calls are diagnostic only: no
common8 calibration, band widening or biological validation. A new complete
canonical calibration is deferred until this pilot demonstrates utility.

## Resources and deliverables

Existing warm native worker and pooled ORCA runners; one32CPU/200000MiB H200
allocation for sixteen searches and a later≤16cross evaluations, CPU-only
64CPU/128GiB eight8-rank solver allocation for≤64GFN calls. Finite manifests,
actual receipts/cost, source/candidate coordinates, tests/report/vault. No
arbitrary project time cap; ordinary scheduler policies apply. Shared executor
changes are owned by root and limited to a protocol-validation dispatch.
