# General polar-contact motion envelope: preparation phase

## Authorization and scientific question

Jacob's standing contained-improvement authorization applies. Parent explicitly
authorized this preparation branch after the complete three-source transfer and
A8 existing-output audit. The question is whether a physically declared search
envelope can make three-source context membership robust to permitted local
motions. This is not an A8-specific fragment rescue or evidence of missing clashes.
Only preparation, mapping and exact archive inventory run in this phase. No new
energies, forces, geometry searches, DFT, folds, protonation, water states or
production changes. Root reviews actual finite pilot counts before submission.

## Frozen selection

Use the same original source-specific donor-anchor set and completed functional
groups as the existing context selector (direct nonwater core N/O within3.3 Å,
then its existing bonded carbonyl/carboxylate/PQQ partners). Verify the anchor
identities against each unchanged saved context audit. The sole new selection
parameter is the polar neighbor radius: **4.3 Å =3.5 Å existing shell +0.8 Å
maximum permitted heavy-atom displacement**. N/O/S neighbor eligibility, source
chemistry handling, actual peptide predecessors, disulfide closure and complete
fragment graph/cap/protonation validation remain unchanged. Existing explicit
water inventory remains fixed; record excluded waters and unsupported chemistry.

The reachability argument is limited to these original anchors versus omitted
neighbors that stay at source coordinates while the anchor moves by≤0.8 Å.
It is not a bound for two independently mobile atoms (which would require their
relative-displacement bound), newly eligible donor-anchor identities, all steric
contacts or continuum-solvent convergence. Membership is frozen before search;
no candidate geometry may add/remove fragments or choose another source union.

All100 original three-of-four noncanonical-La subsets remain declared. Preserve
all six prior preparation-unavailable triples; no member replacement or partial
union. For every complete triple, union the source-backed complete fragment
identities discovered at4.3 Å in exactly those three sources. Deduplicate identical
protein/fragment selections and identical source/selection pairs only, keeping
all100 rows and their prescribed membership. Additional preparation failures stay
explicit. No outcome, class label or old energy enters membership selection.

## Canonical and probe inputs

Prepare each of the25 designated canonical geometries using the **same previously
frozen first-complete triple** from
`workspaces/union_triple_preparation_20260923/canonical28_v1/SELECTION.json`.
Do not pick a different triple if the larger context fails. Existing1H4I,4MAE and
1KB0 crystal selectors each receive their own singleton4.3 Å context.

Six probes use the same canonical-designated triple for their reference protein,
with no probe source contributing to selection:

- A0A3F2YLY8 Ca-conditioned samples1 and3.
- A0ACD6B9F2 Ca-conditioned sample4 and La-conditioned sample4.
- A8R3S4 La-conditioned samples1 and3, using the already declared triple_008
  (samples1/2/3), the original failing168-atom selection.

The A8 sources already belong to primary preparation; reuse those exact outputs
and retain separate probe roles. Other probes retain their actual source graph.
All probes/crystals are excluded from calibration. Every admitted member must
match the exact selected source-atom, bond, H-parent, cap-identity, charge and
protein identity of its declared group's deterministic source anchor. Source
coordinates/H positions may differ between real folds; identities/state may not.

Protocol: `Nikasha_three_La_fixed_polar_motion_envelope4p3_preparation_v1`.
No inherited calibration. A prospective canonical28+six-probe pilot will fit only
its25 original canonical cases by the established extrema/minimum-gap rule,
then freeze that reference before any new transfer interpretation.

## Mapping, archive inventory and anticipated molecular scope

Reuse existing `consistent_context.prepare_one`, source graph/cap machinery and
`union_triple_transfer.mapped` kinematics. No changes to released modules or
private historical protocol IDs. Record complete atom/cap/charge/fragment changes
versus the original local, ten-fold and previous three-fold contexts. Validate
actual q0 paired maps and original physical modes before claiming eligibility.

Audit exact existing OMOL origins/forces and candidate pools only if source,
atom order/coordinates, graph/state/cap maps, model and four-mode numerical settings
match. Distinguish physical/MACE reuse from solvent numerical compatibility.
Historical native-GFN cold energies cannot replace a future strict-native recipe
without explicit completed qualification; no strict input is emitted here.

The later pilot, if feasible, uses the same native-vacuum four-mode selector,
SLSQP200 withftol1e−8 Hartree-equivalent,±0.8rad and0.8Å constraints and the common
{origin,adaptive_Ca,adaptive_La} pool. No new starts or solvent-force search.
Its total maximum before exact reuse is34 origins×2 MACE/4 GFN2 and34 source
pools×2 searches/2 cross-MACE/8 candidate GFN2. Actual finite missing/reuse counts
will be sent before any energy calls. Scalar numerical settings remain a separate
qualification decision; preparation does not silently adopt pending strict32
results. Existing warm GPU32CPU/200000MiB and CPU32CPU/64GiB/rank1 stages are the
prospective resources, not current submissions.

## Deliverables and limits

New owned adapter/tests, actual100-triple preparation ledger, canonical28+six-probe
source inventory and finite proposed molecular manifest/reuse ledger. Products
under `workspaces/motion_envelope_20260923/`; compact diagnostics here and a vault
note. Preserve every failed source, old calibration and old result. No molecular
calls in preparation; no accuracy improvement or rollout can be claimed from it.
