# Third-panel figure delivery

Jacob requested only the third plot from the emailed `nikasha_discrimination.png`,
with the legend, and an emailed copy. The original attachment was matched by SHA-256
to `workspaces/discrimination_transfer_figures_20260923/strict_ablation_v1/structural_transfer.png`.

The existing Matplotlib renderer now supports a readable single-panel layout and
explicit display limits. The `union_precision_strict` panel was regenerated from
the exact original comparison ledger. All 225 exported source rows match the
original third-panel rows exactly, with unchanged bands and original shared axis
limits. Visual inspection confirmed complete labels, legend and footnote.
No new scientific calculations, labels, thresholds or default changes.

The output is `workspaces/discrimination_transfer_figures_20260923/accommodation_only_v1/Nikasha_discrimination.png`;
SVG/PDF remain alongside it as `structural_transfer.svg`/`.pdf`.
The PNG was emailed to Jacob. `EMAIL_RECEIPT.json` records local relay acceptance.
