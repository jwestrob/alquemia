# Large-checkpoint transfer of the typed-group vacuum descriptor

Declared before any grouped-large evaluation, under Jacob's standing autonomous
MACE goal. The medium 25-reference calibration continues independently and
unchanged. Its partial outcomes do not change this protocol or decision rule.

## Question and fixed comparison

Does increased model capacity improve the typed-group model's actual La/Ca
directions, especially the remaining RTX/parvalbumin errors, at acceptable cost?
The medium model gave17/22 raw directions, all7 original comparisons correct,
and three grouping shifts above the retained2-model-kcal tolerance. This is
consumed method-development evidence, never blind validation.

Use the installed/pinned MACE-POLAR-1-L checkpoint and its already qualified
analytic real-space backend. Same source preparations, complete atoms, paired
coordinates, formal microstates, waters, typed3.1A groups, three-stage grouped
Fukui restoration and vacuum energy expression as the medium model. Only the
checkpoint changes scientifically. Retain the existing large-memory tile sizes
(2048 edges/128 nodes) rather than medium4096/256; these are numerical tiling
controls, not a new Hamiltonian. No new software install, solvent, DFT, geometry
optimization, training, labels, aquo reference or calibration threshold.

New protocol: intact_POLAR_large_typed31_group_vacuum_compatibility_v1.
No medium-model absolute band is transferable to this checkpoint.

## Inventory and execution

Reuse all34site group preparations from the completed medium transfer. Exactly
57newMACEevaluations: two native-identity controls on GGR1GLG (Ca/La, one charge
group, exact old large-coordinate ordering), then the same55endpoint tasks as
the medium study. The latter comprise14original primary endpoints,32multisite
endpoints,6alternate-GGR-group endpoints and3RTX rigid/permutation checks.
The55coordinates and scientific state/group settings must match the medium
manifest exactly. Reuse actual native-large GGR receipts for the identity
comparison; those archived energies are not substituted for grouped predictions.

Use one additional A5000/16CPUs/64474MiB through the existing runner. Prior large
full-protein calls used about16GB allocated/22GB reserved GPU memory; large PQQ
endpoints were about121s. Estimate35–55GPUminutes for this inventory, with actual
receipts including failures and overhead. This is a finite experiment inventory,
not a project time/CPU cap. Do not interrupt the running medium calibration.

## Frozen interpretation and checks

Keep all22 directional comparisons and their strata unchanged: PQQ1,
alpha/GGR6, Khoury means9, parvalbumin sites6. Aequorin stays an ordered unlabeled
three-site vector. Nine biological groups, eight directionally/supporting
labeled. Report every case and failed/unavailable calculation.

R=E_Ca-E_La, one eV-to-model-kcal conversion. Compare gauge-independent margins
between proteins, not absolute large-minus-medium R values. A directional
improvement requires more than17/22 correct and retention of all7 original
directions; no broad-validation claim follows from this small consumed panel.
Report losses as well as gains, grouping sensitivity and measured cost.

Before grouped calls, the two one-group controls must reproduce archived native
large energies within0.01modelkcal, forces within0.001eV/A and density coefficients
within1e-8. Failed identity stops this manifest rather than evaluating a different
model as success. Retain charge/group closure1e-5e, Fukui cancellation ratio1e-10,
rigid/permutation energy0.01modelkcal and force0.001eV/A, and all three grouping
contrast tolerances2modelkcal. No tolerance or model changes after inspection.
Utility and qualification remain separately reported. Preserve every previous
result, the baseline/default, and immutable execution snapshots.
