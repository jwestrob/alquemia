# Consistent context, solvent physics and fair adaptive transfer

## Authorization and ownership

Jacob approved all three directions after the September22 delivery:
"lets address all that, including adaptive accommodation."
This covers the contained implementation, numerical qualification, canonical
calibration and existing-fold comparisons below. Preserve production, explicit
DFT, prior experiments, states and labels. No new folds, new DFT, PLM cohort
rescore, publication or default promotion. No project-wide time/compute ceiling;
every job nevertheless has a finite task manifest and actual cost receipt.

Root integrates scores, references, comparison and submissions across branches.
Second_shell owns consistent-context preparation/adapter; khoury_benchmark owns
the matched CPCM solvent adapter; water_basins owns numerical adaptive completion.
Each edits new owned files. Root owns shared pool/comparison changes. Notify root
of exact allocation/manifest before launching to prevent duplicate work or CPU
oversubscription. Coordination is not another user approval gate.

## A. Same local atom membership across real structural replicas

For each original reference protein, select the union of source-backed complete
context-fragment identities across all its supported existing folds, without
using metal-class labels or calculated energies. Preserve the canonical core,
cofactor/water/proton inventory, source heavy coordinates and graph-based caps.
Enforce the same physical atom identities, chemical state and charge per protein
across admitted folds; do not average coordinates or invent absent atoms.
Existing failed sources remain in the full250 denominator. Incompatible mappings
or chemical states remain explicit rather than being silently repaired.

First evaluate all saved folds of four declared groups: A0A3F2YLY8, A0ACD6B9F2,
Q9Z4J7, Q88JH5, including both folding metals, together with all25 canonical
calibration geometries. These groups include current errors and Ca guardrails;
they are development cases selected before new energies, not blind tests.
There are61 unique sources in this first union. Then apply the frozen viable
model to all250 (25 calibration,225 primary transfer), reusing the first stage.
The three existing crystal controls are singleton-union reconstruction checks,
kept separately from the250fold denominator; reuse exact compatible cells where
possible (at most6MACE and12GFN2 additional calls).
If preparation or canonical calibration fails, report that failure and the
available raw contrasts rather than claiming calibrated transfer.

Energy remains native OMOL vacuum plus native GFN2 ALPB-minus-vacuum at exactly
paired coordinates. This is a changed context/preparation protocol and gets its
own calibration. A changed cavity/charge/atom inventory is not attributed solely
to geometry. No new optimization in this branch. Maximum before reuse/failure:
500 MACE and1000 GFN2 endpoints across250sources; initial61 at most122/244.

## B. Fixed-coordinate solvent model comparison

Compare the actual installed native GFN2 CPCM(water) contribution with ALPB(water)
on the same existing context coordinates and electronic states:

`E_M,new = E_OMOL,vac,M + E_GFN2,CPCM,M − E_GFN2,vac,M`.

No second full solvent energy, adjusted coefficient or fitted dielectric/radius.
Verify actual parsed CPCM operation and declared default cavity settings before
class claims. The initial four contexts are1H4I,4MAE, canonicalQ9Z4J7 and
canonicalA0A3F2YLY8 (eight metal endpoints). Reuse existing MACE/vacuum only with
matching states/recipes. If CPCM needs a different native SCF path, qualify it
and recompute matching vacuum contributions as necessary; do not silently mix
incompatible results. Declare any such paired numerical pilot before execution.
An unsupported solver route remains blocked with no manufactured scalar.

After coherent execution, finish the original25canonical+3crystal+2unknownPLM
population and then225 primary folds with a frozen canonical-only reference.
Maximum target solvent endpoints510, plus explicitly necessary matched vacuum
recomputations and numerical controls. New DFT is not part of this branch.
Do not combine CPCM with the new context or adaptive search in the first test:
isolate each change against the current released context scorer.

## C. Complete adaptive accommodation under its own reference

Old-band regression does not alone reject a changed scorer. Complete the missing
reference case using one generic, versioned numerical policy for unsafe SLSQP
trials, preserving the same four force-selected motions, original start, native
MACE energy/analytic gradients,0.8Å final domain and±0.8radian bounds.
No fake objective value, gradient at another coordinate, loosened overlap guard,
new chemical state or label-specific optimizer. Test on all original30 under
the same policy, not just MMOL1770; retain all prior failures and old results.

Initial eight-endpoint numerical pilot uses1H4I,4MAE,MMOL1770,Q9Z4J7. Then
complete the original30, calibrate exactly the original25, freeze the reference,
and evaluate all225 primary folds under the same selection/optimizer policy.
Each source uses its common origin/previous terminal proposals plus the two new
adaptive candidates. All admitted geometries are cross-scored for both metals
with native GFN2 ALPB-minus-vacuum endpoints. Reuse compatible completed cells;
failed required cells remain unavailable. No cross-fold minimum or population
interpretation. Existing final-geometry and native-energy acceptance remain.

This is at most510 optimizer starts across255sources before unavailable inputs,
plus510 cross-MACE and2040 GFN2 singlepoints before deduplication/reuse. Optimizer
iteration rules are finite in each declared numerical protocol; there is no
new project-total budget. Start with pilot/calibration before larger transfer.

## Common scientific comparison and decisions

Use the existing25canonical members and the established class-extrema rule,
including its0.02model-kcal/mol minimum gap, separately for each new protocol.
All25 are required. Freeze references before inspecting corresponding fold
outputs; no test-set threshold tuning. Preserve old-band transfer explicitly.

Compare raw R, own-calibrated decisions, individual Ca/La contributions, coverage,
preparation/solver failures and actual cost with the released static context and
preservedDFT on identical covered sources. Keep correct/wrong/inconclusive/
unavailable separate. Summarize all225 and Ca/La conditioning arms, the existing
strict La4/Ca5 summaries and100 correlated La triples; never drop missing members.
Source spread is a structural descriptor, not uncertainty or entropy.

Success requires an actual benefit in known-class decisions or structural
robustness with reference fidelity, coverage and practical cost visible. A wider
training gap, more abstentions, reduced force, or shifting an unknownPLM prediction
is insufficient by itself. Null or negative branches close independently.
No automatic production promotion follows a favorable development result.
