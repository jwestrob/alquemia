# Primary225 preparation under the same scaled numerical policy

Preparation is authorized in parallel with original30 completion and calibration.
Execution remains unlaunched until root freezes the separate adaptive reference.

Retain exactly all225 declared noncanonical reference folds, with450 metal-endpoint
status rows. Use the completed original shared-pool collection to establish
which existing three-geometry base pools are available. Its20 unavailable cases
(17 preparation failures and3 numerical base failures) remain explicit and get
no new proposal task. All205 available base cases receive the same paired-q0
four-angle selector and scaled native-MACE optimizer:410 manifested searches.
This eligibility rule uses preparation/computation availability, not a score,
classification or favorable displacement. Source/replicate groups remain intact.

Reuse the existing physical maps, exact source coordinates and actual archived
native q0 forces. Reproject those same forces onto the selected four primitives;
do not confuse the old terminal-only gradient vector with the new four-mode
vector. The original source point, force receipt, map and new projection are
recorded. No protein, water, protonation, composition, atom or formal charge changes.
Keep the already established1e−12 Å numerical coordinate reconciliation policy.

The physics and numerical settings are identical to
`common_four_angular_native_OMOL_SLSQP_hartree_units_v2` on original30. Only
population/source-adapter metadata differ. Every candidate is generated from
its original q0 under its own metal energy, one start. No extra starts or fallback.
All missing/failed cases remain in the denominator; an empty task list never
makes a case successful. No new fold, DFT, solvent, fit or reference construction
occurs in preparation. Root owns matching five-candidate pool execution,
canonical-only reference and all transfer conclusions. These folds have already
been consumed in development; do not call the transfer prospectively blind.
