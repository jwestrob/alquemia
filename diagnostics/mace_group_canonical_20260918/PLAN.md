# Typed-group full-protein PQQ calibration attempt

Declared before new model evaluations under Jacob's standing discretionary
MACE-goal authorization. Preserve every previous result and production default.

## Question and unchanged model

Can `intact_POLAR_medium_typed31_group_vacuum_compatibility_v1` separate the
25 canonical PQQ functional-class references and transfer to the two already
consumed crystal structures? This is calibration of an empirical electronic
compatibility descriptor, not an affinity or free-energy calculation.

Use exactly the completed typed-group transfer model: medium POLAR checkpoint,
native trained readouts, validated analytic kernels, three-stage grouped Fukui
restoration, typed 3.1 A complete-residue/actual-amide/disulfide grouping, complete
PQQ(-3), fixed full-protein source coordinates and protonation. No solvent,
optimization, training, new DFT, adjusted charge scheme or change to the group
policy. The three previous GGR grouping tests remain failed at the unchanged
2-model-kcal tolerance. Study predictive usefulness alongside that limitation;
do not promote the model or report qualified broad accuracy.

## Inventory, labels and finite execution scope

Use all 25 existing full preparations in
`workspaces/mace_omol_20260917/intact_panel_prepared_v2/` and authoritative labels
from the pinned canonical inventory and its original calibration release.
Eleven La-class and fourteen Ca-class proteins, no exclusions. Keep source
assembly, explicit-water inventory (empty here), PQQ state and all atom mappings.
The panel is consumed calibration data, not independent or blind validation.
Crystal 1H4I and 4MAE are retrospective structural transfers and sequence
replicates of P16027 and I0JWN7, respectively. Reuse their four actual endpoint
receipts from the identical typed-group model, after method/state verification.
1KB0 remains explicitly unsupported for full-protein scoring because TRO is
unparameterized; keep this third transfer in the denominator as unavailable.

Exactly 50 new MACE endpoint evaluations, two per calibration case. Existing
runner, one A5000 with 16 allocated CPUs and 64474 MiB host RAM. Previous PQQ
measurements were approximately 57 seconds per endpoint; estimate around one
GPU-hour including overhead, with actual receipts controlling the final report.
No project time or CPU budget. A failure remains visible and only technical
recovery preserving the declared scientific state is automatic.

## Frozen score, calibration rule and checks

R = (E_Ca - E_La) * eV_to_model_kcal, converted exactly once. No aquo-reference
substitution or inherited baseline threshold. Larger R means more La-like.
Require all 25 pairs and numerical charge checks to pass before publishing
calibration bands. Let C=max(R of fourteen Ca cases) and L=min(R of eleven La
cases). Only if L-C > 0.02 model kcal, define Ca as R<=C, La as R>=L, and the
open interval as inconclusive. If the classes overlap, report calibration
unavailable; do not remove an outlier or force a separating threshold. Report
all 154 Ca/La pairwise orderings and the complete per-case scores regardless.
The four cached crystal endpoints never enter the calibration extrema.

Retain total/group charge tolerance 1e-5 e and adapter near-cancellation failure
ratio 1e-10; source pairing/coordinates/group membership must match exactly
(stored contact-distance norm roundoff tolerance 1e-12 A). Reuse the already
executed rigid/permutation checks as declared prior evidence; do not imply new
per-case invariance runs. Retain the failed grouping checks separately from
numeric execution, and leave production-qualified decisions unavailable.

Report baseline results side by side using their own released bands. Neither
calibration accuracy nor improved panel spread proves information beyond motif,
charge and composition, which already separate this panel. Preserve evidence
strata, structural/sequence replication and all missing-case denominators.
