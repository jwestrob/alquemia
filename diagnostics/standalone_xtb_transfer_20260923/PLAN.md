# Frozen standalone STATIC transfer to225 structural repeats

Root explicitly authorized STATIC-only transfer after the new backend preserved
25/25 canonical and3/3 crystal calls under its own reference. The standalone
minimal adaptive variant failed canonical separation and is closed; it supplies
no decisions or extra candidates here. Jacob's Sept23 overnight authorization
covers this contained experiment.

## Source population and finite execution

Retain all225 primary noncanonical reference folds, grouped by the existing25
protein identities and Ca/La conditioning arms. Use all208 physically prepared
q0contexts with416 actual matching nativeMACE energy receipts. Seventeen real
preparation failures remain explicitly unavailable. The old nativeGFN failure
atA8R3S4 Ca-sample3 La must not gate the new backend's valid physical input.
No new structures/protonation/water changes, geometry optimization, MACE or DFT.

208origins ×2metals ×2media =832logical standalone cells. Four exactA0A3F2YLY8
Ca-sample1 origin cells were already evaluated ataccuracy0.02 in the224pilot;
verify/reuse them. **828 new low-level calls**, one fresh attempt each, no retry
or third accuracy. All source coordinates/caps/charges/singlets stay fixed.
Physical source records and actual MACE receipts were independently inventoried
in standalone_xtb_reference_20260923/TRANSFER_INPUT_INVENTORY_v1.json.

Use exactly the validated standalonexTB6.7.1/GFN2 recipe:accuracy0.02,
fresh/no-restart,electronic300K,500SCC cap,zero unpaired electrons, both standalone
vacuum andALPBwater. Preserve explicitgsolv/normal230point surface/noionic
screening and version-pinned defaults(P16,GBOBC,solvent298.15K,dielectric80.2,
H-bond/refshift terms). No external/native mixed solvation or backend fallback.
One64CPU/128GiB CPU-sharing allocation,eight8-thread workers,zeroGPU. Preserve
all attempts/actual timing; finite task count is not a project time budget.

## Immutable reference and full-denominator comparison

Use ONLY the available `static` variant of the completed canonical reference:
`workspaces/standalone_xtb_reference_20260923/run_v1/REFERENCE_v1.json`.
FrozenCa maximum−405466.16155897034,La minimum−405456.4020100691model kcal/mol.
No new bands, labels, source selection or zero are fitted to transfer outcomes.
E=nativeOMOL(vac)+standaloneGFN2(ALPB−vacuum),R=E_Ca−E_La. All four cells per
source required; unavailable never becomes a native-baseline success.

Compare all225 and common available coverage against the archived released
native static, DFT and completed native minimal-adaptive recovery ledgers.
Preserve their distinct inputs/units/bands and historical failure records.
The two recovered native adaptive sources remain only in that comparator; no
fresh standalone adaptive scoring is performed. DFT is a different core/model
comparison, not the same-endpoint component of the standalone score.

Reuse the existing strictLa4,Ca5,balanced-arm and everyLa3-of-4 summaries with
all required members; missing members invalidate their group. Keep all25groups
and100triples, raw contrasts/decision transitions and coverage denominators.
Do not call225structural repeats independent biological observations or these
already inspected families prospective blind validation. The rule/reference
is frozen before this new backend's fold scores are inspected.

Report actual calls/reuse/failures/cost, classification results, common-coverage
transitions and source-condition effects. Source-state/solver qualification
remains distinct from predictive utility. No production/default change or
further optimization is automatically authorized by a favorable result.
