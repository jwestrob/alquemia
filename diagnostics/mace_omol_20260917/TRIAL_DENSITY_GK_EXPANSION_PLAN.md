# Frozen responsive-density hybrid: GGR structure transfer

Declared after the two-group development pass in TRIAL_DENSITY_GK_REPORT.md,
before new quantum, MACE or environmental scores on these structures. Jacob's
active autonomous goal authorizes this work. Baseline and prior records stay
unchanged. Candidate physics is frozen at commit 5f7d164 and its execution pins.

## Question and fixed comparison

Does the passing hybrid retain alpha-over-GGR ordering on both previously
inspected alternative GGR structures, 2FW0 and 2FVY? These structures defeated
the earlier masked-MACE descriptor. Both remain consumed structural robustness
cases within the same GGR biological group, not independent or blind labels.

Use exactly the archived source-backed whole-protein preparations
`ggr_source_bridge_2fw0_v1/preparation.json` and
`ggr_source_bridge_2fvy_v1/preparation.json` under the existing MACE workspace,
and their archived stage-B `alpha_caps/preparation_manifest.json` cores.
The alpha-cap policy is the same extended peptide representation used by the
passing 1GLG comparison. Preserve every source heavy coordinate, mapped
normalized protein H, cap geometry, charge, microstate, source chain and zero
waters. No connected-core alternative or conformer search is added in this test.

The source-specific exclusions remain explicit: 2FW0 sodium/citrate/malonate;
2FVY glucose/acetate/CO2/glycerol. Both lack terminal residues 1 and 307–309;
2FVY has reported Glu149 radiation damage. These test transfer across the
already declared preparations, not a causal sugar effect or a complete crystal
assembly. No omitted species is silently assigned a guessed parameter.

Compare both new R values with **both** frozen alpha values. Require all four
new differences >0.02 kcal/mol, preserving the earlier four comparisons as
separate historical development results. Also report the full three-structure
GGR range and all six alpha-versus-GGR structural comparisons (one extended
1GLG representation per crystal); do not pick a favorable structure or average
away a failure. The connected 1GLG partition control remains separately reported.
No absolute zero, old baseline band or fitted threshold is applied.

## Same scientific model; reusable input preparation

Reuse the actual source graph, generic peptide repair, H normalization,
barycentric cap projection, local residue charge closure and fixed ff19SB
generating field. The new preparation interface takes explicit source records
and a case configuration rather than requiring edits to source-code case lists.
Replay 1GLG preparation against the passing archive before admitting new cases.
Pure preparation never invents quantum charges or an unavailable correction.

Run the same native ORCA 6.1.1 r2SCAN-3c/DefGrid3/TightSCF analytic-gradient
endpoint policy in that permanent field, `DoEQ false`, no CPCM. Two endpoints
per new structure; no additional vacuum DFT is required to evaluate the frozen
trial-density functional. Subtract the actual generating-field interaction once.
Without a fresh vacuum endpoint, the old variational-response diagnostic is
explicitly unavailable, not assumed to pass. Parse all native state/ECP/energy
and gradient checks; these gradients are not combined hybrid gradients.

Same medium MACE-POLAR checkpoint, exact short-readout adapter, float64,
full-minus-core algebra. Same CHELPG grid/extent/radii, no renormalization or
per-case charge-model choice. Same AMOEBA2018/Tinker backend, full physical
boundary, common metal radius, source mask, residue charge redistribution,
direct quantum multipoles and coupled GK induction. Same energy expression
and all numerical tolerances from the passing protocol; no fit to new results.

## Declared inventory and execution

- Preparation/source replay, exact paired invariants and explicit exclusions;
  two native framework parameter reads and eight source/reference boundary
  initializations. These do not calculate energies or forces.
- Four new embedded DFT endpoints total, including analytic gradients. Preserve
  GBW/density files needed downstream. Failed attempts and retries remain visible.
- Eight MACE short forwards: full/core × Ca/La × two structures. Existing
  compatible receipts may be reused only after exact source/backend verification.
- Four native CHELPG fits and four saved-density potential queries. Each query
  includes fixed exterior quality probes and the same 49-point observations at
  every actual environment site, including old-field subtraction centers.
- Same primary, rigid, ±5% common-metal-radius variants and response refinement:
  27 static energies (including three vacuum identity states), 24 field queries,
  30 response solves. No quantum recomputation for these numerical controls.
- Collect all statuses and unrounded components; retain the same numerical,
  projection, field-quality and large-response checks. Missing/failed states
  cannot be replaced by baseline or old-geometry output.

Use established manifested runners and isolated pinned environments. DFT uses
64 CPU ranks allocated as four simultaneous 16-rank endpoints. Prior eight
embedded endpoints took 655 wall seconds /41,920 allocated core-seconds;
this is scale evidence, not a promised duration or project budget. Density
utilities use one CPU worker per endpoint; native response uses the established
CPU implementation. Eight short forwards fit the already qualified standard
GPU path; no H200 or training is required. Record preparation and failed costs,
wall/CPU/GPU/RSS and reused upstream costs separately. No project CPU/time cap.

Candidate products: `workspaces/mace_omol_20260917/trial_gk_expansion_*`.
The first runnable phase may submit quantum work after real preparation tests;
downstream absence stays explicit until its components execute. Do not submit
an unimplemented or placeholder dependency. Protocol family remains the frozen
responsive-density/GK/MACE expression; new preparation/runner IDs identify the
general interface without silently changing historical input hashes.

## Wider benchmark inventory, without premature labels

After structural transfer, add independently grouped proteins through explicit
preparation support. Parvalbumin CD/EF require retained background calcium and
the actual acetyl group and remain cross-study support. Aequorin stays an ordered
EF1/EF3/EF4 vector without invented site labels. Canonical PQQ transfer requires
explicit cofactor boundary/radii and its own evidence stratum. Inventory these
inputs now; their additional scoring manifests will be declared before launch.
Do not quietly omit their unsupported atoms to pass the single-metal adapter.

This transfer test cannot finish broad validation. Preserve either outcome,
report cost and failures, and decide the next informative test from the evidence.
