The first local preparation stopped before writing any task because the recipe
check treated an archived provenance comment as the keyword line. No energy
calculation or submission occurred. The corrected check ignores comment/blank
lines, requires the exact native keyword line and rejects unexpected method
blocks. Existing maxcore is an execution-memory setting controlled by the pinned
runtime renderer. Failed prepared_v1 and first preflight output remain preserved;
prepared_v2 passes the corrected check with identical declared scientific inputs.
