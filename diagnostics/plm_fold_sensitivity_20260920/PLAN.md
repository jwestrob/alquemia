# Two preselected PLM targets: three-fold prediction sensitivity

## Authorization and question

After the real reference CLI check passed, parent explicitly authorized these two
isolated development requests under Jacob's overnight goal. Does fold-to-fold
variation alone change the two compressed-site predictions, or does the concern
persist across all three saved structures? Physical donor-response work is a
separate ongoing experiment. These PLM proteins have no verified biological
La/Ca labels; this is prediction sensitivity, not accuracy validation.

## Fixed sources and method before submission

Use exactly the two source requests previously recorded by root in
`workspaces/accommodation_goal_20260920/plm_source_requests_v1/`:

- PQQSEQ_07ab500e3df76b30d71c: all three saved seed101 samples0/1/2.
- PQQSEQ_83440678cbbd658047c9: all three saved seed101 samples0/1/2.

Both requests retain original source hashes, actual sequences/numbering,
La/PQQ selectors and the prior site-role mapping. The first target's sample2
previously failed single-fold admission. It is included unchanged and must not
be replaced if current preparation fails. No additional target, alternate fold,
water/protonation change, donor motion, folding, DFT or threshold adjustment.

Two independent standard plans use the released fixed-core→complete-context
preparation, native OMOL + [nativeGFN2ALPBwater−vacuum], then the opt-in declared
three-fold median. Maximum12MACE +24GFN2 fresh endpoints; each three-member group
requires all three compatible successful scores. A failed group stays unavailable,
while the independent other group may proceed. No forced cache/import of old
scientific outputs. Same H20032CPU/200000MiB wrapper conventions, no project time cap.

All member raw scores/components and context composition changes, domain flags,
prior admission failures, current preparation failures and actual costs remain
visible. The unchanged single-structure bands transfer to the median explicitly
as developmental. Large spread is reported, not hidden by aggregation. The
standard production scan/default and all original PLM results remain untouched.

For sample0 compare prior same-model/method outputs from
`workspaces/accommodation_torsion_20260920/primary_result_v1.json` and its pinned
point0 preparation only when actual context coordinates, charges, model and
GFN2 method match exactly. Otherwise record preparation mismatch; do not force
agreement, reuse or interpret unlike scores as pure numeric error. This is not
an instruction to wait for or duplicate the separate DFT torsion calculations.
