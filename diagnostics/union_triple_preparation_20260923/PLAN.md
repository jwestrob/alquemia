# Three-source fragment unions: preparation-only qualification

## Authorized scope

Parent explicitly authorized reconstructing all100 existing three-of-four
noncanonical La-conditioned subsets, preserving all six old unavailable triples,
plus one separately reported A0A3 Ca-sample1 stress probe. Jacob's contained
improvement authorization remains in force. No molecular energy/force calls,
optimization, new folds, protonation, water changes, scheduler jobs or promotion.

## Frozen selection and physical preparation

Use the hash-pinned read-only inventory at
`workspaces/union_subset_inventory_20260923/inventory_v1.json` and its original250
source preparation. Every protein contributes all four combinations of its four
noncanonical La sources. No canonical or Ca source contributes to triple membership.
A triple with an old unsupported member remains unavailable; do not form a partial
union or substitute another source. For each complete triple, union the actual
source-backed fragment identities from its three old local contexts. Freeze every
union before geometry reconstruction. Deduplicate only identical protein/fragment
selections and identical source/selection pairs; retain all100 triple rows.

The read-only inventory implies94 complete triples,34 distinct selections and125
source/selection reconstructions (16 changed selections/55 pairs). These are
planning counts, not proof of atom-graph equivalence. Use unchanged
`consistent_context.prepare_one` graph closure, source H/caps, covalent bonds,
cofactor/core/waters, charge and paired coordinate checks. Compare physical state
identity within each declared selection and actual coordinates against the old
ten-fold contexts. A deterministic first source is a graph/state-check anchor,
not a calibration member or excluded fold. All failures remain explicit.

The single separate stress probe is
`a0a3f2yly8-pqq-la_model__conditioned_Ca__seed-1_sample-1`, reconstructed with the
single unique A0A3 La-triple fragment selection. No Ca geometry contributes to
that membership. It does not count among the100 triples or their125 pairs.

## Reuse and interpretation

Protocol: `Nikasha_three_La_source_fragment_union_preparation_v1`.
Map existing native OMOL scalar energies/forces and native GFN2 vacuum/ALPB
receipts only after matching source identity, complete atom graph/state, paired
XYZ/order, checkpoint and actual receipt method. Exact ten-fold matches may
reuse original origin values; smaller fragment lists do not imply a changed
physical graph until reconstruction proves it. Also inspect compatible original
local-context receipts. Preserve original receipt IDs rather than relabeling
history. Adaptive candidate geometries are not silently reused: their physical
maps/settings require a separate explicit check.

No threshold fitting or new calibrated reference. Any later use of the old
union/released/adaptive bands on changed contexts is a developmental transfer,
not a new validated calibration. This delivery reports actual geometry/reuse
counts and missing numerical work only; it makes no new accuracy claim.

## Products

New script/test and this compact diagnostic directory; all geometries and complete
source/selection/reuse records under `workspaces/union_triple_preparation_20260923/`.
Actual wall/CPU time, zero molecular-call count, real-fixture tests and vault note.
