# Solvent-consistent whole-group accommodation profiles

Authorized under Jacob's overnight goal and permission to pursue promising
contained directions. This is a development intervention, not blind validation.

## Question

Does the solvent-aware composite energy distinguish compressed donor geometry
from a stable alternative organization, and does its local response agree with
native DFT? The earlier vacuum-MACE optimizer and averaged radial-force feature
did not provide robust gains. This experiment isolates named whole-carboxylate
motions and uses the same solvent-aware Hamiltonian at every point.

## Fixed source set and intervention

Use original prepared1H4I and4MAE controls, plus the two previously identified
compressed PLM sources PQQSEQ_83440678cbbd658047c9 and
PQQSEQ_07ab500e3df76b30d71c, selected sample0 in both. Their extreme existing
scores were already inspected; they have no biological truth labels. The earlier
PLM geometry review found compression in every saved sample. Preserve the current
chemical state, complete PQQ, water count, charges and source scaffold.

Construct the existing complete polar context, without an energy-dependent
selection rule. Use exact existing physical rotation maps and cap chain rules:
terminal anchor-Glu chi3 for all4cases; terminal extra-Asp chi2 for4MAE and both
PLM cases. Displace one mode at a time by[-0.4,-0.2,0,+0.2,+0.4]radian
(up to23degrees); every other mode remains zero. No minimization, fitted spring,
target metal distance or desired coordination number. The seven paths share four
origins, giving32unique geometries and64metal endpoints. Failed mappings,
unsupported context chemistry, or severe new overlaps remain explicit failures.

Evaluate all64endpoints with native float64OMOL and each with nativeGFN2 vacuum
andALPBWater (128low-level calls), using the existing composite recipe. Original
refined-state coordinates and caps are not mixed in. The two known control
origins must reproduce the earlier composite inputs/results. No new calibration
or production score is generated.

## Native validation and interpretation

If preparation and primary execution are valid, validate the extra-Asp paths at
both±0.2radian on all3cases, both metals:12native r2SCAN-3c/CPCM/DefGrid3
singlepoints on the SAME expanded contexts. Add the4PLM origin endpoints;
reuse the exact4MAE context origins. This is16new high-level endpoints total,
not an optimization campaign. Freeze these identities before profile results;
do not select favorable scan points afterward. Actual endpoint cost determines
resource sizing; this one-time validation is not proposed routine scanner cost.

Report metal-specific work, differential work, per-mode gradients/curvature
diagnostics, metal–donor geometry, overlaps and representation invariants.
Negative curvature and boundary-descending profiles stay visible; do not clamp
them or invent harmonic relaxation/entropy. A profile minimum in this truncated
domain is not a binding free energy. Do not assign PLM accuracy from agreement
with an expected XoxF label. A transferable score requires subsequent known-label
structural-robustness testing; that independent inventory is in progress.

All calculations use existing runners, finite manifests and immutable receipts.
DFT/reference/default promotion remains owned by the separate release task.
