# Explicit three-source envelope preparation

Parent assignment under Jacob's active discretionary discriminator goal:
“Prepare a thin opt-in practical three-source adapter with explicit
inputs/reference ... Start with existing two PLM three-source request fixtures;
preparation/dry-run only, no molecular calls or favorable class expectations.”

Use the exact declared AF3 La-conditioned samples0/1/2 for
PQQSEQ_07ab500e3df76b30d71c and PQQSEQ_83440678cbbd658047c9. Reuse their
archived original protonation/preparation, explicit selectors/configuration and
source hashes. Both biological labels remain unknown. No new folds, protonation,
energies, gradients, search, threshold fit or production changes.

The existing motion-envelope kernel selects source-connected complete fragments
within4.3Å of the original donor anchors, independently on each source; the fixed
group includes the union of all three lists. This is the existing3.5Å radius plus
the allowed0.8Å maximum heavy-atom displacement, not a claim of complete steric or
electrostatic coverage. Anchors must replay exactly. The physical-state anchor is
the lexicographically first declared source ID, fixed before energies. All three
must share source protein/atom/cofactor/proton/cap/bond/charge/water state. Any
unsupported member invalidates the group's execution handoff; no reduced union.

Use the actual envelope canonical25 reference from
`workspaces/motion_envelope_20260923/REFERENCE_PINNED_v1.json`, never prior threefold or
tenfold bands. It separates canonical25 and retains three crystal calls; five of
six consumed probes are correct and A0A3Ca3 is inconclusive. It supports a named
experimental candidate, not a qualified production model. Missing/incompatible/
nonseparating reference emits no molecular tasks.

New module provides request, prepare, dry-run and report. It calls existing source
validators, envelope discovery/preparation and physical-map kernels. Preserve all
older public APIs and immutable preparations. Generic execution is deliberately
absent at this checkpoint; later integration will reuse warm nativeMACE and strict
GFN2 executors, preserving their numerical profile and missing-cell behavior.

For each completely prepared triple, the prospective finite scope is six fresh
nativeMACE origin-force calls, six bounded four-mode searches, at most six
cross-MACE calls and36 strict scalar GFN2 calls (12origins+24candidates). Across
these two triples:12+12+12MACE operations and72GFN2 scalar calls maximum; the number
of native evaluations inside the twelve bounded searches is data dependent.
No cache energy/force reuse is claimed. Existing protonation is reused explicitly.
Scalar recipe: freshNoAutostart, native300K, TolE1e-10Eh, MaxIter500, rank1;
vacuum andALPBwater matched. MACE/selector/optimizer/profile/reference are pinned.
The future report must retain per-source original/accommodated contrasts,
mathematical/operational selections and strict median/spread of all three sources.

This plan authorizes only coordinate reconstruction, integrity checks, task/input
staging, tests and documentation. Molecular integration remains held for root's
separate decision after structural-transfer evidence.
