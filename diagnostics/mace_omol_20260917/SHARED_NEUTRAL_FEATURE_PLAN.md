# Next bounded candidate: a shared learned neutral charge feature

Declared after the raw-zero feature's response and subtractive partition
failures, before any evaluation of this alternative. Active-goal authorization
applies. This is a distinct empirical descriptor, not a change to physical
protein charge or the production baseline.

## Reason for this single alternative

Native OMOL's system-wide charge conditioning previously failed global/remote
spectator tests. Removing its raw charge vector gave a useful PQQ descriptor,
but the vector of zeros is not necessarily a learned molecular state. Its
response and core replacement now fail physical development checks. Test one
specific alternative: feed the checkpoint's actual charge-zero embedding into
the joint projection for BOTH metal endpoints and ALL atoms, keeping the native
spin embedding and all parameters unchanged. The shared vector does not vary
with protein size, net charge or metal choice.

Physical charges, electron counts, coordinates, protonation, water inventories
and multiplicities remain recorded and unchanged. Override only the documented
learned categorical feature; do not set the actual protein's charge to zero or
claim a new physical electronic state. Do not sweep other charge categories or
choose a different feature per protein. The prior raw-zero descriptor and its
reference remain immutable and accessible.

Candidate ID: `omol_shared_neutral_charge_feature_descriptor_v1`.
Verify the installed charge-zero category mapping against the existing embedding
inspection; do not assume that tensor index0 means physical charge0. This
alternative still lacks validated long-range electrostatics and is not a quantum
or binding-free-energy calculation.

## Initial eight-call core experiment

Use exactly the eight centers of the completed masked response screen:
GGR_extended, GGR_connected, ALPHA_1F6S and ALPHA_6IP9, each Ca/La. No geometry
change, optimization, new DFT, solvent solver or training. One native-method
analytic-gradient forward per endpoint with the separately qualified exact
checkpoint adapter, float64 and physical source/cap mappings. Initial inventory:
eight new model calls, one A5000/16CPU/64474MiB, roughly a minute or two including
startup based on measured core costs. Count actual attempts and receipts.

These four actual La core states have formal charge0. For them the new feature
must reproduce an unmodified native charge0 calculation. Audit the existing
OMOL benchmark for exact coordinate, charge, spin, checkpoint and software
matches and reuse its outputs where valid. Require energy agreement<=0.01model
kcal, gradient max-component agreement<=0.001eV/A, preserved model parameters
and native readout closure. If an exact native reference is absent, record the
missing qualification and declare a specific recomputation before execution.
Do not invent the reference or borrow a differently prepared core.

Retain actual projected gradients versus the archived DFT gradients as diagnostic
information; eight centers alone do not validate curvature or relaxation.
The core-size subtraction test uses the same predeclared2kcal-scale threshold
and connected-minus-extended algebra as MASKED_SUBTRACTIVE_CONTEXT_PLAN.md.
No biological labels or energy-dependent parameter choice enters the adapter.
All cases are consumed method-development cases, not independent blind tests.

If numerical equivalence fails, stop dependent scoring and diagnose it. If the
partition test fails, do not run the conditional static-hybrid whole-protein
stage for this candidate. Record the failed candidate; no charge-category sweep.
If it passes, prepare a finite follow-on manifest for the same three original-H,
source-matched physical proteins (six energy-only calls), with the same fixed
DFT-core-plus-full-minus-core expression and four relative-order criteria in
MASKED_SUBTRACTIVE_CONTEXT_PLAN.md, substituting this new descriptor consistently.
This conditional inventory is six additional calls, not permission for a broad
rescore. No old PQQ bands or universal zero transfers to the new descriptor.
A pass is development evidence only; broad accuracy remains a separate question.
