# Does the demonstrated adaptive gain require the older proposals?

Authorized by Jacob's latest discretionary overnight experiment instruction,
recorded in `../overnight_discrimination_20260923/CURRENT.md`. Root owns this
report-only branch. Scope fixed before inspecting the reduced-pool results.

The completed five-candidate adaptive method repairs a reference-fold error and
two inconclusives, but its cost and inherited failures prevent routine use. Test
exactly one reduced candidate set: **origin, adaptive_Ca, adaptive_La**. Both
metals see this identical set. Drop the two older terminal-only proposals
uniformly for every source, irrespective of energies, class or failure. Keep
the same physical four-mode proposal algorithm, coordinates, states and
MACE + native GFN2(ALPB−vacuum) components. No new molecular calls or optimization.

Inputs are the immutable original30 and primary225 collections in
`workspaces/adaptive_completion_20260922/{original30_pool_v1,primary225_pool_v1}/`.
Use their existing `final_collection.json` and preserve all unscorable cases.
Require all six metal/candidate matrix cells for a reduced score. Do not turn
an unavailable search into an origin-only success. Record missing inherited
preparation/proposal reasons separately; this branch does not repair them.

Retain mathematical minima and the unchanged operational 0.1 kcal/mol origin
retention policy. Preserve full five-candidate results beside the reduced pool.
Energy selection remains independent of class labels. Components have their
actual archived numerical policy; this does not inherit qualification from the
new continuation experiments or claim that a solver issue is absent.

First use only the original25 designated canonical calibration members and the
existing class-extrema/minimum-gap rule to freeze a separately named reduced-pool
reference. The other three crystal and two PLM examples are excluded from
calibration. Then apply that reference unchanged to all225 consumed noncanonical
folds, retaining released-band and five-candidate-band transfer separately.
No threshold is adjusted after the fold comparison. These are development data,
not a newly blind biological test; unknown PLM preferences remain unknown.

Report all correct/wrong/inconclusive/unavailable counts, pairwise common
coverage versus released and five-candidate scoring, all changed decisions,
raw contrast changes, selected geometries and endpoint accommodation works.
Reuse the existing strict La4, Ca5, balanced-arm and every La three-of-four
summary rules. No favorable source selection or probability/affinity claim.

Three distinct candidates nominally require12 native solvent calls versus20 for
five; report this only as call-count arithmetic, not measured cold latency.
Existing optimization and all previous compute are reused, not erased from
historical cost. No production change or promotion follows automatically.

If the reduced pool preserves the useful accuracy/robustness gain, next consider
making its preparation independent of failed older proposals. Any fresh search
would have a separate finite manifest and is not executed by this replay.
